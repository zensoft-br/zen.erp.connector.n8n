import type { IExecuteFunctions, IDataObject } from 'n8n-workflow';
import { request } from '../../transport/request';

export const operations: Record<
  string,
  Record<string, (this: IExecuteFunctions, i: number) => Promise<any>>
> = {

  "auth": {

      "login": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_authLogin', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_authLogin', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_authLogin', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/auth/login`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "logout": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_authLogout', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_authLogout', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/auth/logout`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "me": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_authMe', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_authMe', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/auth/me`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "passwordRecover": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_authPasswordRecover', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_authPasswordRecover', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/auth/passwordRecover`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "passwordReset": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_authPasswordReset', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_authPasswordReset', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/auth/passwordReset`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "refresh": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_authRefresh', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_authRefresh', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/auth/refresh`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "register": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_authRegister', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_authRegister', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_authRegister', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/auth/register`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }

  },
  "catalog": {

      "categoryCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalogCategoryCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalogCategoryCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalogCategoryCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/category`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "categoryRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalogCategoryRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalogCategoryRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/category`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "categoryUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalogCategoryUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalogCategoryUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalogCategoryUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/category`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "categoryDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalogCategoryDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalogCategoryDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalogCategoryDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/category/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "categoryReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalogCategoryReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalogCategoryReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalogCategoryReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/category/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "catalog/company": {

      "companyCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/companyCompanyCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/companyCompanyCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/companyCompanyCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/company/company`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "companyRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/companyCompanyRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/companyCompanyRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/company/company`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "companyUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/companyCompanyUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/companyCompanyUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/companyCompanyUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/company/company`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "companyDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/companyCompanyDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/companyCompanyDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/companyCompanyDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/company/company/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "companyReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/companyCompanyReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/companyCompanyReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/companyCompanyReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/company/company/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "companyOpGetActive": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/companyCompanyOpGetActive', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/companyCompanyOpGetActive', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/catalog/company/companyOpGetActive`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "companyOpSetActive": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/companyCompanyOpSetActive', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/companyCompanyOpSetActive', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/companyCompanyOpSetActive', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/catalog/company/companyOpSetActive/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "societyCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/companySocietyCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/companySocietyCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/companySocietyCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/company/society`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "societyRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/companySocietyRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/companySocietyRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/company/society`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "societyUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/companySocietyUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/companySocietyUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/companySocietyUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/company/society`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "societyDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/companySocietyDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/companySocietyDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/companySocietyDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/company/society/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "societyReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/companySocietyReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/companySocietyReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/companySocietyReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/company/society/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "catalog/location": {

      "cityCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/locationCityCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationCityCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/locationCityCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/location/city`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "cityRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/locationCityRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationCityRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/location/city`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "cityUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/locationCityUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationCityUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/locationCityUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/location/city`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "cityDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/locationCityDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/locationCityDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationCityDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/location/city/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "cityReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/locationCityReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/locationCityReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationCityReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/location/city/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "cityOpFind": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/locationCityOpFind', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationCityOpFind', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/locationCityOpFind', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/location/cityOpFind`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "countryCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/locationCountryCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationCountryCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/locationCountryCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/location/country`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "countryRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/locationCountryRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationCountryRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/location/country`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "countryUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/locationCountryUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationCountryUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/locationCountryUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/location/country`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "countryDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/locationCountryDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/locationCountryDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationCountryDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/location/country/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "countryReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/locationCountryReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/locationCountryReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationCountryReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/location/country/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "geocodeRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/locationGeocodeRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationGeocodeRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/location/geocode`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stateCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/locationStateCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationStateCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/locationStateCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/location/state`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stateRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/locationStateRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationStateRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/location/state`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stateUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/locationStateUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationStateUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/locationStateUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/location/state`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stateDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/locationStateDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/locationStateDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationStateDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/location/state/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stateReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/locationStateReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/locationStateReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationStateReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/location/state/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "zipcodeRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/locationZipcodeRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/locationZipcodeRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/location/zipcode`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "catalog/person": {

      "personCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/personPersonCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/person/person`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/person`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/personPersonUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/person/person`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/person/person/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/person/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personAddressCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonAddressCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonAddressCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/personPersonAddressCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/person/personAddress`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personAddressRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonAddressRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonAddressRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/personAddress`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personAddressUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonAddressUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonAddressUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/personPersonAddressUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/person/personAddress`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personAddressDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonAddressDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonAddressDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonAddressDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/person/personAddress/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personAddressReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonAddressReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonAddressReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonAddressReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/personAddress/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personAddressOpSetDefault": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonAddressOpSetDefault', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonAddressOpSetDefault', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonAddressOpSetDefault', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/catalog/person/personAddressOpSetDefault/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personCommentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonCommentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonCommentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/personPersonCommentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/person/personComment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personCommentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonCommentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonCommentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/personComment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personCommentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonCommentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonCommentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/personPersonCommentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/person/personComment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personCommentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonCommentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonCommentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonCommentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/person/personComment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personCommentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonCommentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonCommentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonCommentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/personComment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personCompactRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonCompactRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonCompactRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/personCompact`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personCompactReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonCompactReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonCompactReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonCompactReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/personCompact/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personContactCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonContactCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonContactCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/personPersonContactCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/person/personContact`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personContactRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonContactRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonContactRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/personContact`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personContactUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonContactUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonContactUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/personPersonContactUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/person/personContact`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personContactDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonContactDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonContactDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonContactDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/person/personContact/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personContactReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonContactReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonContactReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonContactReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/personContact/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personDocumentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonDocumentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonDocumentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/personPersonDocumentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/person/personDocument`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personDocumentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonDocumentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonDocumentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/personDocument`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personDocumentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonDocumentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonDocumentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/personPersonDocumentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/person/personDocument`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personDocumentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonDocumentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonDocumentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonDocumentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/person/personDocument/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personDocumentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonDocumentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonDocumentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonDocumentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/personDocument/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personGroupCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonGroupCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonGroupCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/personPersonGroupCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/person/personGroup`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personGroupRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonGroupRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonGroupRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/personGroup`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personGroupUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonGroupUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonGroupUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/personPersonGroupUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/person/personGroup`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personGroupDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonGroupDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonGroupDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonGroupDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/person/personGroup/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personGroupReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/personPersonGroupReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/personPersonGroupReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonGroupReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/person/personGroup/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personOpReadFromPersonDirectory": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/personPersonOpReadFromPersonDirectory', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/personPersonOpReadFromPersonDirectory', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/catalog/person/personOpReadFromPersonDirectory`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "catalog/product": {

      "codeConversionCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productCodeConversionCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productCodeConversionCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productCodeConversionCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/product/codeConversion`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "codeConversionRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productCodeConversionRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productCodeConversionRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/codeConversion`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "codeConversionUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productCodeConversionUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productCodeConversionUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productCodeConversionUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/product/codeConversion`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "codeConversionDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productCodeConversionDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productCodeConversionDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productCodeConversionDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/product/codeConversion/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "codeConversionReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productCodeConversionReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productCodeConversionReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productCodeConversionReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/codeConversion/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "codeConversionListCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productCodeConversionListCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productCodeConversionListCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productCodeConversionListCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/product/codeConversionList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "codeConversionListRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productCodeConversionListRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productCodeConversionListRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/codeConversionList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "codeConversionListUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productCodeConversionListUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productCodeConversionListUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productCodeConversionListUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/product/codeConversionList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "codeConversionListDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productCodeConversionListDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productCodeConversionListDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productCodeConversionListDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/product/codeConversionList/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "codeConversionListReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productCodeConversionListReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productCodeConversionListReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productCodeConversionListReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/codeConversionList/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dimensionCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productDimensionCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productDimensionCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productDimensionCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/product/dimension`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dimensionRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productDimensionRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productDimensionRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/dimension`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dimensionUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productDimensionUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productDimensionUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productDimensionUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/product/dimension`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dimensionDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productDimensionDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productDimensionDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productDimensionDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/product/dimension/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dimensionReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productDimensionReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productDimensionReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productDimensionReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/dimension/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/product/product`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/product`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/product/product`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/product/product/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/product/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productKitComponentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductKitComponentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductKitComponentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductKitComponentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/product/productKitComponent`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productKitComponentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductKitComponentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductKitComponentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/productKitComponent`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productKitComponentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductKitComponentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductKitComponentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductKitComponentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/product/productKitComponent`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productKitComponentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductKitComponentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductKitComponentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductKitComponentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/product/productKitComponent/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productKitComponentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductKitComponentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductKitComponentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductKitComponentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/productKitComponent/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productOpClone": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductOpClone', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductOpClone', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductOpClone', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductOpClone', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/product/productOpClone/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productOpConvertToUnit": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductOpConvertToUnit', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductOpConvertToUnit', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductOpConvertToUnit', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/catalog/product/productOpConvertToUnit/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productPackingCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductPackingCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductPackingCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductPackingCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/product/productPacking`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productPackingRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductPackingRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductPackingRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/productPacking`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productPackingUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductPackingUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductPackingUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductPackingUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/product/productPacking`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productPackingDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductPackingDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductPackingDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductPackingDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/product/productPacking/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productPackingReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductPackingReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductPackingReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductPackingReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/productPacking/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productPackingOpConvertToUnit": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductPackingOpConvertToUnit', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductPackingOpConvertToUnit', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductPackingOpConvertToUnit', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/catalog/product/productPackingOpConvertToUnit/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productProfileCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductProfileCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductProfileCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductProfileCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/product/productProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productProfileRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductProfileRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductProfileRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/productProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productProfileUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductProfileUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductProfileUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductProfileUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/product/productProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productProfileDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductProfileDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductProfileDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductProfileDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/product/productProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productProfileReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductProfileReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductProfileReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductProfileReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/productProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productUnitConversionCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductUnitConversionCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductUnitConversionCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductUnitConversionCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/product/productUnitConversion`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productUnitConversionRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductUnitConversionRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductUnitConversionRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/productUnitConversion`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productUnitConversionUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductUnitConversionUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductUnitConversionUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductUnitConversionUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/product/productUnitConversion`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productUnitConversionDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductUnitConversionDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductUnitConversionDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductUnitConversionDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/product/productUnitConversion/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productUnitConversionReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductUnitConversionReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductUnitConversionReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductUnitConversionReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/productUnitConversion/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productVariantCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductVariantCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductVariantCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductVariantCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/product/productVariant`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productVariantRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductVariantRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductVariantRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/productVariant`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productVariantUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productProductVariantUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductVariantUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productProductVariantUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/product/productVariant`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productVariantDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductVariantDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductVariantDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductVariantDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/product/productVariant/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productVariantReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productProductVariantReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productProductVariantReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productProductVariantReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/productVariant/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "unitCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productUnitCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productUnitCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productUnitCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/catalog/product/unit`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "unitRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productUnitRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productUnitRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/unit`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "unitUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_catalog/productUnitUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productUnitUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_catalog/productUnitUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/catalog/product/unit`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "unitDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productUnitDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productUnitDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productUnitDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/catalog/product/unit/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "unitReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_catalog/productUnitReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_catalog/productUnitReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_catalog/productUnitReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/catalog/product/unit/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "commercial/contract": {

      "contractCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/contractContractCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contract`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/contract/contract`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/contractContractUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/commercial/contract/contract`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/commercial/contract/contract/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/contract/contract/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractBillingCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/contractContractBillingCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractBilling`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractBillingRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/contract/contractBilling`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractBillingUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/contractContractBillingUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/commercial/contract/contractBilling`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractBillingDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractBillingDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/commercial/contract/contractBilling/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractBillingReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractBillingReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/contract/contractBilling/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractBillingOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractBillingOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractBillingOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractBillingOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractBillingOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractBillingOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractBillingOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractBillingOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractBillingOpCreateInvoice": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingOpCreateInvoice', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingOpCreateInvoice', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/contractContractBillingOpCreateInvoice', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractBillingOpCreateInvoice`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractBillingOpCreateInvoiceRevert": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingOpCreateInvoiceRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingOpCreateInvoiceRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractBillingOpCreateInvoiceRevert`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractBillingOpCreateRevert": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingOpCreateRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingOpCreateRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractBillingOpCreateRevert`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractBillingOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractBillingOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractBillingOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractBillingOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractBillingOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractBillingOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractBillingOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractBillingOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractIndexCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractIndexCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractIndexCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/contractContractIndexCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractIndex`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractIndexRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractIndexRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractIndexRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/contract/contractIndex`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractIndexUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractIndexUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractIndexUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/contractContractIndexUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/commercial/contract/contractIndex`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractIndexDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractIndexDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractIndexDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractIndexDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/commercial/contract/contractIndex/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractIndexReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractIndexReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractIndexReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractIndexReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/contract/contractIndex/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/contractContractItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/contract/contractItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/contractContractItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/commercial/contract/contractItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/commercial/contract/contractItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/contract/contractItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractOpAdjust": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractOpAdjust', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractOpAdjust', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractOpAdjust', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractOpAdjust/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractOpCancel": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractOpCancel', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractOpCancel', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractOpCancel', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractOpCancel/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractOpCancelRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractOpCancelRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractOpCancelRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractOpCancelRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractOpCancelRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/contractContractOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractProfileCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractProfileCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractProfileCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/contractContractProfileCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/contract/contractProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractProfileRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractProfileRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractProfileRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/contract/contractProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractProfileUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/contractContractProfileUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractProfileUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/contractContractProfileUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/commercial/contract/contractProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractProfileDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractProfileDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractProfileDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractProfileDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/commercial/contract/contractProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractProfileReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/contractContractProfileReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/contractContractProfileReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/contractContractProfileReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/contract/contractProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "commercial": {

      "personHierarchyCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercialPersonHierarchyCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPersonHierarchyCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercialPersonHierarchyCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/personHierarchy`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personHierarchyRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercialPersonHierarchyRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPersonHierarchyRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/personHierarchy`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personHierarchyUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercialPersonHierarchyUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPersonHierarchyUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercialPersonHierarchyUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/commercial/personHierarchy`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "personHierarchyDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercialPersonHierarchyDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercialPersonHierarchyDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPersonHierarchyDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/commercial/personHierarchy/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personHierarchyReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercialPersonHierarchyReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercialPersonHierarchyReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPersonHierarchyReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/personHierarchy/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "personHierarchyViewRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercialPersonHierarchyViewRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPersonHierarchyViewRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/personHierarchyView`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListChangeRequestCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercialPriceListChangeRequestCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListChangeRequestCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercialPriceListChangeRequestCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/priceListChangeRequest`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceListChangeRequestRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercialPriceListChangeRequestRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListChangeRequestRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/priceListChangeRequest`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListChangeRequestDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercialPriceListChangeRequestDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercialPriceListChangeRequestDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListChangeRequestDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/commercial/priceListChangeRequest/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListChangeRequestReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercialPriceListChangeRequestReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercialPriceListChangeRequestReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListChangeRequestReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/priceListChangeRequest/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListChangeRequestOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercialPriceListChangeRequestOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercialPriceListChangeRequestOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListChangeRequestOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/priceListChangeRequestOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListChangeRequestOpReject": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercialPriceListChangeRequestOpReject', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercialPriceListChangeRequestOpReject', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListChangeRequestOpReject', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/priceListChangeRequestOpReject/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListOpFind": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercialPriceListOpFind', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercialPriceListOpFind', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListOpFind', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/priceListOpFind/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListTransformationCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercialPriceListTransformationCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListTransformationCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercialPriceListTransformationCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/priceListTransformation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceListTransformationRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercialPriceListTransformationRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListTransformationRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/priceListTransformation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListTransformationUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercialPriceListTransformationUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListTransformationUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercialPriceListTransformationUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/commercial/priceListTransformation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceListTransformationDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercialPriceListTransformationDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercialPriceListTransformationDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListTransformationDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/commercial/priceListTransformation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListTransformationReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercialPriceListTransformationReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercialPriceListTransformationReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListTransformationReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/priceListTransformation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListTransformationOpCalculate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercialPriceListTransformationOpCalculate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercialPriceListTransformationOpCalculate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercialPriceListTransformationOpCalculate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/commercial/priceListTransformationOpCalculate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "commercial/target": {

      "targetCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/targetTargetCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/target/target`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "targetRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/target/target`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "targetUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/targetTargetUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/commercial/target/target`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "targetDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/targetTargetDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/commercial/target/target/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "targetReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/targetTargetReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/target/target/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "targetGroupCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetGroupCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetGroupCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/targetTargetGroupCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/target/targetGroup`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "targetGroupRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetGroupRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetGroupRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/target/targetGroup`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "targetGroupUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetGroupUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetGroupUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/targetTargetGroupUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/commercial/target/targetGroup`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "targetGroupDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/targetTargetGroupDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetGroupDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetGroupDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/commercial/target/targetGroup/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "targetGroupReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/targetTargetGroupReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetGroupReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetGroupReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/target/targetGroup/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "targetIntervalCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetIntervalCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetIntervalCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/targetTargetIntervalCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/target/targetInterval`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "targetIntervalRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetIntervalRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetIntervalRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/target/targetInterval`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "targetIntervalUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetIntervalUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetIntervalUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/targetTargetIntervalUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/commercial/target/targetInterval`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "targetIntervalDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/targetTargetIntervalDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetIntervalDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetIntervalDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/commercial/target/targetInterval/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "targetIntervalReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/targetTargetIntervalReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetIntervalReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetIntervalReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/target/targetInterval/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "targetPlanCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetPlanCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetPlanCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/targetTargetPlanCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/commercial/target/targetPlan`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "targetPlanRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetPlanRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetPlanRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/target/targetPlan`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "targetPlanUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetPlanUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetPlanUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_commercial/targetTargetPlanUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/commercial/target/targetPlan`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "targetPlanDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/targetTargetPlanDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetPlanDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetPlanDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/commercial/target/targetPlan/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "targetPlanReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_commercial/targetTargetPlanReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_commercial/targetTargetPlanReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_commercial/targetTargetPlanReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/commercial/target/targetPlan/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "financial/accounting": {

      "accountCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/account`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/account`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/accounting/account`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/accounting/account/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/account/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountCategoryCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountCategoryCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountCategoryCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountCategoryCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/accountCategory`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountCategoryRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountCategoryRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountCategoryRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/accountCategory`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountCategoryUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountCategoryUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountCategoryUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountCategoryUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/accounting/accountCategory`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountCategoryDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountCategoryDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountCategoryDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountCategoryDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/accounting/accountCategory/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountCategoryReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountCategoryReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountCategoryReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountCategoryReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/accountCategory/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountChartCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountChartCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountChartCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountChartCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/accountChart`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountChartRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountChartRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountChartRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/accountChart`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountChartUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountChartUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountChartUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountChartUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/accounting/accountChart`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountChartDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountChartDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountChartDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountChartDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/accounting/accountChart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountChartReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountChartReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountChartReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountChartReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/accountChart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountChartOpClone": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountChartOpClone', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountChartOpClone', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountChartOpClone', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/accounting/accountChartOpClone/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountChartOpImport": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountChartOpImport', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountChartOpImport', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountChartOpImport', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountChartOpImport', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/accountChartOpImport/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountChartOpLevelCreate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountChartOpLevelCreate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountChartOpLevelCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountChartOpLevelCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/accounting/accountChartOpLevelCreate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountChartOpLevelDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountChartOpLevelDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountChartOpLevelDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountChartOpLevelDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/accounting/accountChartOpLevelDelete/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountChartOpLevelUpdate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountChartOpLevelUpdate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountChartOpLevelUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountChartOpLevelUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/accounting/accountChartOpLevelUpdate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountChartOpNextShortCode": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountChartOpNextShortCode', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountChartOpNextShortCode', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountChartOpNextShortCode', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/accounting/accountChartOpNextShortCode/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountConversionCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountConversionCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountConversionCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountConversionCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/accountConversion`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountConversionRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountConversionRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountConversionRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/accountConversion`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountConversionUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountConversionUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountConversionUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountConversionUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/accounting/accountConversion`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountConversionDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountConversionDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountConversionDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountConversionDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/accounting/accountConversion/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountConversionReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountConversionReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountConversionReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountConversionReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/accountConversion/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountConversionChartCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountConversionChartCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountConversionChartCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountConversionChartCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/accountConversionChart`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountConversionChartRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountConversionChartRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountConversionChartRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/accountConversionChart`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountConversionChartUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountConversionChartUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountConversionChartUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountConversionChartUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/accounting/accountConversionChart`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountConversionChartDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountConversionChartDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountConversionChartDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountConversionChartDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/accounting/accountConversionChart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountConversionChartReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountConversionChartReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountConversionChartReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountConversionChartReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/accountConversionChart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountDefaultCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountDefaultCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountDefaultCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountDefaultCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/accountDefault`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountDefaultRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountDefaultRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountDefaultRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/accountDefault`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountDefaultUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountDefaultUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountDefaultUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountDefaultUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/accounting/accountDefault`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountDefaultDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountDefaultDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountDefaultDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountDefaultDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/accounting/accountDefault/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountDefaultReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAccountDefaultReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountDefaultReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountDefaultReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/accountDefault/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountDefaultOpRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountDefaultOpRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountDefaultOpRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingAccountDefaultOpRead', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/accountDefaultOpRead`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountFiscalYearRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAccountFiscalYearRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAccountFiscalYearRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/accountFiscalYear`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "assetInventoryRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAssetInventoryRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAssetInventoryRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/assetInventory`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "assetInventoryCurrentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAssetInventoryCurrentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAssetInventoryCurrentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/assetInventoryCurrent`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "assetTagRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingAssetTagRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAssetTagRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/assetTag`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "assetTagReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingAssetTagReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingAssetTagReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingAssetTagReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/assetTag/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalYearOpClose": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingFiscalYearOpClose', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingFiscalYearOpClose', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingFiscalYearOpClose', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/accounting/fiscalYearOpClose/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "journalEntryCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalEntryCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalEntryCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingJournalEntryCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/journalEntry`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "journalEntryRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalEntryRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalEntryRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/journalEntry`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "journalEntryUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalEntryUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalEntryUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingJournalEntryUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/accounting/journalEntry`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "journalEntryDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingJournalEntryDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalEntryDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalEntryDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/accounting/journalEntry/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "journalEntryReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingJournalEntryReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalEntryReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalEntryReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/journalEntry/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "journalEntryOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalEntryOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalEntryOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingJournalEntryOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/journalEntryOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "journalEntryOpPublish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingJournalEntryOpPublish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalEntryOpPublish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalEntryOpPublish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/accounting/journalEntryOpPublish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "journalEntryOpPublishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingJournalEntryOpPublishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalEntryOpPublishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalEntryOpPublishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/accounting/journalEntryOpPublishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "journalEntryOpUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalEntryOpUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalEntryOpUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingJournalEntryOpUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/journalEntryOpUpdate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "journalItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingJournalItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/journalItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "journalItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/journalItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "journalItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingJournalItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/accounting/journalItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "journalItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingJournalItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/accounting/journalItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "journalItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingJournalItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingJournalItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingJournalItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/journalItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "ledgerItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingLedgerItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingLedgerItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/ledgerItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "ledgerItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingLedgerItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingLedgerItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingLedgerItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/ledgerItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "ledgerItemBalanceRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingLedgerItemBalanceRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingLedgerItemBalanceRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/ledgerItemBalance`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "resultCenterCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingResultCenterCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/resultCenter`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "resultCenterRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/resultCenter`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "resultCenterUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingResultCenterUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/accounting/resultCenter`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "resultCenterDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingResultCenterDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/accounting/resultCenter/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "resultCenterReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingResultCenterReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/resultCenter/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "resultCenterChartCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterChartCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterChartCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingResultCenterChartCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/accounting/resultCenterChart`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "resultCenterChartRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterChartRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterChartRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/resultCenterChart`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "resultCenterChartUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterChartUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterChartUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/accountingResultCenterChartUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/accounting/resultCenterChart`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "resultCenterChartDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingResultCenterChartDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterChartDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterChartDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/accounting/resultCenterChart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "resultCenterChartReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingResultCenterChartReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterChartReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterChartReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/accounting/resultCenterChart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "resultCenterChartOpLevelCreate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingResultCenterChartOpLevelCreate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterChartOpLevelCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterChartOpLevelCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/accounting/resultCenterChartOpLevelCreate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "resultCenterChartOpLevelDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingResultCenterChartOpLevelDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterChartOpLevelDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterChartOpLevelDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/accounting/resultCenterChartOpLevelDelete/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "resultCenterChartOpLevelUpdate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/accountingResultCenterChartOpLevelUpdate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/accountingResultCenterChartOpLevelUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/accountingResultCenterChartOpLevelUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/accounting/resultCenterChartOpLevelUpdate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "financial/billing": {

      "batchRequestCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/billingBatchRequestCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchRequest`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "batchRequestRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/billing/batchRequest`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchRequestUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/billingBatchRequestUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/billing/batchRequest`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "batchRequestDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchRequestDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/billing/batchRequest/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchRequestReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchRequestReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/billing/batchRequest/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchRequestOpAddInstructions": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchRequestOpAddInstructions', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestOpAddInstructions', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestOpAddInstructions', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/billingBatchRequestOpAddInstructions', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchRequestOpAddInstructions/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "batchRequestOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchRequestOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchRequestOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchRequestOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchRequestOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchRequestOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchRequestOpCreateFile": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchRequestOpCreateFile', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestOpCreateFile', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestOpCreateFile', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchRequestOpCreateFile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchRequestOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchRequestOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchRequestOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchRequestOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchRequestOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchRequestOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchRequestOpSend": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchRequestOpSend', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestOpSend', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestOpSend', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchRequestOpSend/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchRequestOpSendRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchRequestOpSendRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchRequestOpSendRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchRequestOpSendRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchRequestOpSendRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchResponseRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingBatchResponseRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchResponseRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/billing/batchResponse`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchResponseDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchResponseDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchResponseDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchResponseDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/billing/batchResponse/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchResponseReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchResponseReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchResponseReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchResponseReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/billing/batchResponse/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchResponseOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchResponseOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchResponseOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchResponseOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchResponseOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchResponseOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchResponseOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchResponseOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchResponseOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchResponseOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchResponseOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingBatchResponseOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchResponseOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/billingBatchResponseOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchResponseOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "batchResponseOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchResponseOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchResponseOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchResponseOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchResponseOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchResponseOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchResponseOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchResponseOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchResponseOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchResponseOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "batchResponseOpProcess": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingBatchResponseOpProcess', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingBatchResponseOpProcess', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingBatchResponseOpProcess', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/batchResponseOpProcess/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionRequestRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionRequestRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionRequestRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/billing/instructionRequest`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionRequestUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionRequestUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionRequestUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/billingInstructionRequestUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/billing/instructionRequest`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "instructionRequestDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingInstructionRequestDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionRequestDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionRequestDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/billing/instructionRequest/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionRequestReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingInstructionRequestReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionRequestReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionRequestReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/billing/instructionRequest/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionRequestOpAddToBatch": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingInstructionRequestOpAddToBatch', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionRequestOpAddToBatch', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionRequestOpAddToBatch', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/instructionRequestOpAddToBatch/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionRequestOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionRequestOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionRequestOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/billingInstructionRequestOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/billing/instructionRequestOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "instructionRequestOpCreateChangeDueDate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionRequestOpCreateChangeDueDate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionRequestOpCreateChangeDueDate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/instructionRequestOpCreateChangeDueDate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionRequestOpCreateRegister": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionRequestOpCreateRegister', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionRequestOpCreateRegister', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/instructionRequestOpCreateRegister`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionRequestOpCreateUnregister": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionRequestOpCreateUnregister', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionRequestOpCreateUnregister', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/instructionRequestOpCreateUnregister`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionRequestOpRemoveFromBatch": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingInstructionRequestOpRemoveFromBatch', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionRequestOpRemoveFromBatch', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionRequestOpRemoveFromBatch', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/instructionRequestOpRemoveFromBatch/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionResponseCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionResponseCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionResponseCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/billingInstructionResponseCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/billing/instructionResponse`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "instructionResponseRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionResponseRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionResponseRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/billing/instructionResponse`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionResponseUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionResponseUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionResponseUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/billingInstructionResponseUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/billing/instructionResponse`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "instructionResponseDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingInstructionResponseDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionResponseDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionResponseDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/billing/instructionResponse/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionResponseReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingInstructionResponseReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionResponseReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionResponseReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/billing/instructionResponse/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionResponseOpIgnore": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingInstructionResponseOpIgnore', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionResponseOpIgnore', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionResponseOpIgnore', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/instructionResponseOpIgnore/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionResponseOpIgnoreRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingInstructionResponseOpIgnoreRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionResponseOpIgnoreRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionResponseOpIgnoreRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/instructionResponseOpIgnoreRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "instructionResponseOpProcess": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingInstructionResponseOpProcess', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingInstructionResponseOpProcess', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingInstructionResponseOpProcess', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/instructionResponseOpProcess/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "walletCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingWalletCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingWalletCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/billingWalletCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/billing/wallet`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "walletRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingWalletRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingWalletRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/billing/wallet`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "walletUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/billingWalletUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingWalletUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/billingWalletUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/billing/wallet`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "walletDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingWalletDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingWalletDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingWalletDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/billing/wallet/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "walletReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingWalletReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingWalletReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingWalletReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/billing/wallet/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "walletOpGetSequence": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/billingWalletOpGetSequence', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/billingWalletOpGetSequence', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/billingWalletOpGetSequence', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/billing/walletOpGetSequence/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "financial/credit": {

      "creditLineCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/creditCreditLineCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/creditCreditLineCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/creditCreditLineCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/credit/creditLine`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "creditLineRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/creditCreditLineRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/creditCreditLineRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/credit/creditLine`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "creditLineUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/creditCreditLineUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/creditCreditLineUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/creditCreditLineUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/credit/creditLine`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "creditLineDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/creditCreditLineDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/creditCreditLineDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/creditCreditLineDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/credit/creditLine/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "creditLineReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/creditCreditLineReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/creditCreditLineReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/creditCreditLineReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/credit/creditLine/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "creditLineItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/creditCreditLineItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/creditCreditLineItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/creditCreditLineItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/credit/creditLineItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "creditLineItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/creditCreditLineItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/creditCreditLineItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/credit/creditLineItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "creditLineItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/creditCreditLineItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/creditCreditLineItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/creditCreditLineItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/credit/creditLineItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "creditLineItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/creditCreditLineItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/creditCreditLineItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/creditCreditLineItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/credit/creditLineItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "creditLineItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/creditCreditLineItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/creditCreditLineItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/creditCreditLineItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/credit/creditLineItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "financial": {

      "currencyCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialCurrencyCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialCurrencyCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/currency`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "currencyRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialCurrencyRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/currency`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "currencyUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialCurrencyUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialCurrencyUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/currency`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "currencyDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialCurrencyDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialCurrencyDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/currency/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "currencyReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialCurrencyReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialCurrencyReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/currency/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "currencyExchangeRateCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialCurrencyExchangeRateCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyExchangeRateCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialCurrencyExchangeRateCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/currencyExchangeRate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "currencyExchangeRateRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialCurrencyExchangeRateRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyExchangeRateRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/currencyExchangeRate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "currencyExchangeRateUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialCurrencyExchangeRateUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyExchangeRateUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialCurrencyExchangeRateUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/currencyExchangeRate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "currencyExchangeRateDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialCurrencyExchangeRateDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialCurrencyExchangeRateDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyExchangeRateDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/currencyExchangeRate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "currencyExchangeRateReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialCurrencyExchangeRateReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialCurrencyExchangeRateReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyExchangeRateReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/currencyExchangeRate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "currencyExchangeRateOpConvert": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialCurrencyExchangeRateOpConvert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyExchangeRateOpConvert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/currencyExchangeRateOpConvert`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "currencyExchangeRateOpGetExchangeRate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialCurrencyExchangeRateOpGetExchangeRate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyExchangeRateOpGetExchangeRate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/currencyExchangeRateOpGetExchangeRate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "currencyOpReadByCode": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialCurrencyOpReadByCode', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialCurrencyOpReadByCode', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/currencyOpReadByCode`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialPayableCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialPayableCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/payable`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "payableRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialPayableRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/payable`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialPayableUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialPayableUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/payable`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "payableDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/payable/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/payable/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/payableOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/payableOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableOpChangeWallet": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableOpChangeWallet', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableOpChangeWallet', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableOpChangeWallet', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/payableOpChangeWallet/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/payableOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/payableOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableOpRepeat": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableOpRepeat', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableOpRepeat', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableOpRepeat', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialPayableOpRepeat', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/payableOpRepeat/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "payableOpSettle": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialPayableOpSettle', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableOpSettle', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialPayableOpSettle', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/payableOpSettle`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "payableOpUpdate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableOpUpdate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableOpUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableOpUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialPayableOpUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/payableOpUpdate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "payableOpUpdatePrepared": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialPayableOpUpdatePrepared', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableOpUpdatePrepared', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialPayableOpUpdatePrepared', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/payableOpUpdatePrepared`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "payableSettlementRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialPayableSettlementRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableSettlementRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/payableSettlement`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableSettlementDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableSettlementDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableSettlementDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableSettlementDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/payableSettlement/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableSettlementReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableSettlementReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableSettlementReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableSettlementReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/payableSettlement/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableSettlementBillingTitleRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialPayableSettlementBillingTitleRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableSettlementBillingTitleRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/payableSettlementBillingTitle`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableSettlementBillingTitleReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableSettlementBillingTitleReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableSettlementBillingTitleReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableSettlementBillingTitleReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/payableSettlementBillingTitle/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableSettlementMethodRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialPayableSettlementMethodRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableSettlementMethodRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/payableSettlementMethod`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "payableSettlementMethodReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialPayableSettlementMethodReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialPayableSettlementMethodReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialPayableSettlementMethodReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/payableSettlementMethod/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialReceivableCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialReceivableCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/receivable`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "receivableRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialReceivableRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/receivable`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialReceivableUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialReceivableUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/receivable`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "receivableDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialReceivableDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialReceivableDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/receivable/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialReceivableReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialReceivableReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/receivable/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialReceivableOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialReceivableOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/receivableOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialReceivableOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialReceivableOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/receivableOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialReceivableOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialReceivableOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/receivableOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialReceivableOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialReceivableOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/receivableOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableOpRepeat": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialReceivableOpRepeat', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialReceivableOpRepeat', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableOpRepeat', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialReceivableOpRepeat', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/receivableOpRepeat/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "receivableOpSend": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialReceivableOpSend', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableOpSend', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialReceivableOpSend', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/receivableOpSend`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "receivableOpSettle": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialReceivableOpSettle', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableOpSettle', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialReceivableOpSettle', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/receivableOpSettle`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "receivableOpUpdate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialReceivableOpUpdate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialReceivableOpUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableOpUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialReceivableOpUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/receivableOpUpdate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "receivableOpUpdatePrepared": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialReceivableOpUpdatePrepared', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableOpUpdatePrepared', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financialReceivableOpUpdatePrepared', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/receivableOpUpdatePrepared`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "receivableSettlementRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialReceivableSettlementRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableSettlementRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/receivableSettlement`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableSettlementDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialReceivableSettlementDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialReceivableSettlementDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableSettlementDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/receivableSettlement/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableSettlementReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialReceivableSettlementReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialReceivableSettlementReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableSettlementReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/receivableSettlement/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableSettlementBillingTitleRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialReceivableSettlementBillingTitleRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableSettlementBillingTitleRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/receivableSettlementBillingTitle`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableSettlementBillingTitleReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialReceivableSettlementBillingTitleReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialReceivableSettlementBillingTitleReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableSettlementBillingTitleReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/receivableSettlementBillingTitle/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableSettlementMethodRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financialReceivableSettlementMethodRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableSettlementMethodRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/receivableSettlementMethod`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "receivableSettlementMethodReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financialReceivableSettlementMethodReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financialReceivableSettlementMethodReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financialReceivableSettlementMethodReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/receivableSettlementMethod/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "financial/salesCommission": {

      "salesCommissionCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/salesCommissionSalesCommissionCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/salesCommission/salesCommission`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "salesCommissionRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/salesCommission/salesCommission`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "salesCommissionUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/salesCommissionSalesCommissionUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/salesCommission/salesCommission`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "salesCommissionDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/salesCommissionSalesCommissionDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/salesCommission/salesCommission/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "salesCommissionReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/salesCommissionSalesCommissionReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/salesCommission/salesCommission/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "salesCommissionOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/salesCommissionSalesCommissionOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/salesCommission/salesCommissionOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "salesCommissionOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/salesCommissionSalesCommissionOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/salesCommission/salesCommissionOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "salesCommissionOpCalculate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionOpCalculate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionOpCalculate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/salesCommissionSalesCommissionOpCalculate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/salesCommission/salesCommissionOpCalculate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "salesCommissionOpCalculateRevert": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionOpCalculateRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionOpCalculateRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/salesCommissionSalesCommissionOpCalculateRevert', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/salesCommission/salesCommissionOpCalculateRevert`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "salesCommissionOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/salesCommissionSalesCommissionOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/salesCommission/salesCommissionOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "salesCommissionOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/salesCommissionSalesCommissionOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/salesCommission/salesCommissionOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "salesCommissionOpPublish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/salesCommissionSalesCommissionOpPublish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionOpPublish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionOpPublish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/salesCommission/salesCommissionOpPublish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "salesCommissionOpPublishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/salesCommissionSalesCommissionOpPublishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/salesCommissionSalesCommissionOpPublishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/salesCommissionSalesCommissionOpPublishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/salesCommission/salesCommissionOpPublishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "financial/treasury": {

      "accountStatementCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/treasuryAccountStatementCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/treasury/accountStatement`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountStatementRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/treasury/accountStatement`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountStatementUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/treasuryAccountStatementUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/financial/treasury/accountStatement`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountStatementDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/treasuryAccountStatementDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/financial/treasury/accountStatement/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountStatementReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/treasuryAccountStatementReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/financial/treasury/accountStatement/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountStatementOpConsolidate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/treasuryAccountStatementOpConsolidate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementOpConsolidate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementOpConsolidate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/treasuryAccountStatementOpConsolidate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/treasury/accountStatementOpConsolidate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountStatementOpConsolidateRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/treasuryAccountStatementOpConsolidateRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementOpConsolidateRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementOpConsolidateRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/treasury/accountStatementOpConsolidateRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountStatementOpIdentify": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/treasuryAccountStatementOpIdentify', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementOpIdentify', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementOpIdentify', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/treasury/accountStatementOpIdentify/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountStatementOpIdentifyRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/treasuryAccountStatementOpIdentifyRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementOpIdentifyRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementOpIdentifyRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/treasury/accountStatementOpIdentifyRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountStatementOpIgnore": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/treasuryAccountStatementOpIgnore', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementOpIgnore', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementOpIgnore', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/treasury/accountStatementOpIgnore/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountStatementOpIgnoreRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/treasuryAccountStatementOpIgnoreRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementOpIgnoreRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementOpIgnoreRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/treasury/accountStatementOpIgnoreRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accountStatementOpSettle": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/treasuryAccountStatementOpSettle', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementOpSettle', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementOpSettle', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/treasuryAccountStatementOpSettle', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/treasury/accountStatementOpSettle/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accountStatementOpSettleRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_financial/treasuryAccountStatementOpSettleRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_financial/treasuryAccountStatementOpSettleRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryAccountStatementOpSettleRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/financial/treasury/accountStatementOpSettleRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "expenseOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/treasuryExpenseOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryExpenseOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/treasuryExpenseOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/treasury/expenseOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "revenueOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_financial/treasuryRevenueOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_financial/treasuryRevenueOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_financial/treasuryRevenueOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/financial/treasury/revenueOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }

  },
  "fiscal/br": {

      "dfeRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfe`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfe/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcInCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcInCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcInCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/brDfeNfeProcInCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcIn`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dfeNfeProcInRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcInRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcInRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeNfeProcIn`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcInDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcInDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcInDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcInDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/br/dfeNfeProcIn/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcInReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcInReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcInReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcInReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeNfeProcIn/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcInOpIncomingInvoiceCreate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcInOpIncomingInvoiceCreate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcInOpIncomingInvoiceCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcInOpIncomingInvoiceCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/brDfeNfeProcInOpIncomingInvoiceCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcInOpIncomingInvoiceCreate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dfeNfeProcInOpIncomingInvoiceCreateRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcInOpIncomingInvoiceCreateRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcInOpIncomingInvoiceCreateRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcInOpIncomingInvoiceCreateRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcInOpIncomingInvoiceCreateRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeNfeProcOut`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcOutDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/br/dfeNfeProcOut/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcOutReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeNfeProcOut/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutOpCancel": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcOutOpCancel', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutOpCancel', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutOpCancel', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/brDfeNfeProcOutOpCancel', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcOutOpCancel/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutOpConfirm": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcOutOpConfirm', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutOpConfirm', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutOpConfirm', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcOutOpConfirm/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutOpCorrect": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcOutOpCorrect', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutOpCorrect', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutOpCorrect', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/brDfeNfeProcOutOpCorrect', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcOutOpCorrect/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcOutOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutOpDownload": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutOpDownload', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutOpDownload', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/brDfeNfeProcOutOpDownload', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcOutOpDownload`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutOpOutgoingInvoiceCreate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcOutOpOutgoingInvoiceCreate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutOpOutgoingInvoiceCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutOpOutgoingInvoiceCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/brDfeNfeProcOutOpOutgoingInvoiceCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcOutOpOutgoingInvoiceCreate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutOpSign": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcOutOpSign', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutOpSign', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutOpSign', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcOutOpSign/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutOpSignRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcOutOpSignRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutOpSignRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutOpSignRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcOutOpSignRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutOpTransmit": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeNfeProcOutOpTransmit', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutOpTransmit', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutOpTransmit', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcOutOpTransmit/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeNfeProcOutOpUpload": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeNfeProcOutOpUpload', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeNfeProcOutOpUpload', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/brDfeNfeProcOutOpUpload', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeNfeProcOutOpUpload`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dfeOpReadContent": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeOpReadContent', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeOpReadContent', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeOpReadContent', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeOpReadContent/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeProcEventoNFeInRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeProcEventoNFeInRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeProcEventoNFeInRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeProcEventoNFeIn`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeProcEventoNFeInReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeProcEventoNFeInReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeProcEventoNFeInReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeProcEventoNFeInReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeProcEventoNFeIn/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeProcEventoNFeOutRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeProcEventoNFeOutRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeProcEventoNFeOutRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeProcEventoNFeOut`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeProcEventoNFeOutReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeProcEventoNFeOutReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeProcEventoNFeOutReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeProcEventoNFeOutReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeProcEventoNFeOut/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeProcInutNFeCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeProcInutNFeCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeProcInutNFeCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/brDfeProcInutNFeCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeProcInutNFe`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dfeProcInutNFeRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeProcInutNFeRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeProcInutNFeRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeProcInutNFe`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeProcInutNFeReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeProcInutNFeReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeProcInutNFeReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeProcInutNFeReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeProcInutNFe/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeResEventoRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeResEventoRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeResEventoRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeResEvento`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeResEventoReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeResEventoReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeResEventoReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeResEventoReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeResEvento/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeResNFeInRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeResNFeInRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeResNFeInRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeResNFeIn`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeResNFeInReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeResNFeInReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeResNFeInReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeResNFeInReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/br/dfeResNFeIn/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dfeResNFeInOpManifest": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/brDfeResNFeInOpManifest', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/brDfeResNFeInOpManifest', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDfeResNFeInOpManifest', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/br/dfeResNFeInOpManifest/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "documentSefazOpFetch": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brDocumentSefazOpFetch', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brDocumentSefazOpFetch', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/brDocumentSefazOpFetch', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/br/documentSefazOpFetch`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "spedOpExport": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/brSpedOpExport', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/brSpedOpExport', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/brSpedOpExport', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/br/spedOpExport`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }

  },
  "fiscal": {

      "fiscalProfileOperationCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfileOperationCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfileOperationCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalFiscalProfileOperationCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/fiscalProfileOperation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "fiscalProfileOperationRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfileOperationRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfileOperationRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/fiscalProfileOperation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalProfileOperationUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfileOperationUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfileOperationUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalFiscalProfileOperationUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/fiscalProfileOperation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "fiscalProfileOperationDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalProfileOperationDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfileOperationDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfileOperationDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/fiscalProfileOperation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalProfileOperationReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalProfileOperationReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfileOperationReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfileOperationReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/fiscalProfileOperation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalProfileOperationOpReadByCode": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfileOperationOpReadByCode', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfileOperationOpReadByCode', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/fiscalProfileOperationOpReadByCode`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalProfilePersonCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfilePersonCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfilePersonCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalFiscalProfilePersonCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/fiscalProfilePerson`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "fiscalProfilePersonRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfilePersonRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfilePersonRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/fiscalProfilePerson`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalProfilePersonUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfilePersonUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfilePersonUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalFiscalProfilePersonUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/fiscalProfilePerson`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "fiscalProfilePersonDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalProfilePersonDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfilePersonDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfilePersonDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/fiscalProfilePerson/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalProfilePersonReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalProfilePersonReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfilePersonReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfilePersonReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/fiscalProfilePerson/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalProfileProductCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfileProductCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfileProductCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalFiscalProfileProductCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/fiscalProfileProduct`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "fiscalProfileProductRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfileProductRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfileProductRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/fiscalProfileProduct`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalProfileProductUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfileProductUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfileProductUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalFiscalProfileProductUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/fiscalProfileProduct`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "fiscalProfileProductDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalProfileProductDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfileProductDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfileProductDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/fiscalProfileProduct/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalProfileProductReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalProfileProductReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalProfileProductReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalProfileProductReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/fiscalProfileProduct/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalRegionCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalRegionCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalRegionCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalFiscalRegionCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/fiscalRegion`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "fiscalRegionRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalRegionRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalRegionRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/fiscalRegion`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalRegionUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalRegionUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalRegionUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalFiscalRegionUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/fiscalRegion`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "fiscalRegionDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalRegionDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalRegionDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalRegionDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/fiscalRegion/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalRegionReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalRegionReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalRegionReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalRegionReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/fiscalRegion/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalRegionOpReadByCode": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalRegionOpReadByCode', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalRegionOpReadByCode', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/fiscalRegionOpReadByCode`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalYearCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalYearCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalYearCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalFiscalYearCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/fiscalYear`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "fiscalYearRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalYearRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalYearRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/fiscalYear`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalYearUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalYearUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalYearUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalFiscalYearUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/fiscalYear`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "fiscalYearDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalYearDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalYearDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalYearDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/fiscalYear/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalYearReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalYearReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalYearReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalYearReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/fiscalYear/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalYearOpPartialClosure": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalYearOpPartialClosure', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalYearOpPartialClosure', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalYearOpPartialClosure', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/fiscalYearOpPartialClosure/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalYearOpPartialClosureRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalYearOpPartialClosureRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalYearOpPartialClosureRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalYearOpPartialClosureRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/fiscalYearOpPartialClosureRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalYearOpPublish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalYearOpPublish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalYearOpPublish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalYearOpPublish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/fiscalYearOpPublish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalYearOpPublishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalFiscalYearOpPublishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalFiscalYearOpPublishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalYearOpPublishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/fiscalYearOpPublishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fiscalYearOpRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalFiscalYearOpRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalFiscalYearOpRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/fiscalYearOpRead`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalIncomingInvoiceCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoice`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingInvoiceRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/incomingInvoice`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalIncomingInvoiceUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/incomingInvoice`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingInvoiceDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/incomingInvoice/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/incomingInvoice/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalIncomingInvoiceItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingInvoiceItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/incomingInvoiceItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalIncomingInvoiceItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/incomingInvoiceItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingInvoiceItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/incomingInvoiceItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/incomingInvoiceItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceOpApproveDivergences": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceOpApproveDivergences', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceOpApproveDivergences', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceOpApproveDivergences', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceOpApproveDivergences/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalIncomingInvoiceOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingInvoiceOpCreateFromIncomingList": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceOpCreateFromIncomingList', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceOpCreateFromIncomingList', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalIncomingInvoiceOpCreateFromIncomingList', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceOpCreateFromIncomingList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingInvoiceOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceOpReject": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceOpReject', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceOpReject', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceOpReject', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceOpReject/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceOpRejectRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceOpRejectRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceOpRejectRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceOpRejectRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceOpRejectRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceOpReturn": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceOpReturn', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceOpReturn', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceOpReturn', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalIncomingInvoiceOpReturn', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceOpReturn/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingInvoiceOpTaxationCalc": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoiceOpTaxationCalc', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceOpTaxationCalc', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceOpTaxationCalc', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceOpTaxationCalc/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoiceOpUpdatePrepared": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoiceOpUpdatePrepared', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoiceOpUpdatePrepared', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalIncomingInvoiceOpUpdatePrepared', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoiceOpUpdatePrepared`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingInvoicePaymentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoicePaymentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoicePaymentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalIncomingInvoicePaymentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/incomingInvoicePayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingInvoicePaymentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoicePaymentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoicePaymentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/incomingInvoicePayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoicePaymentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoicePaymentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoicePaymentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalIncomingInvoicePaymentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/incomingInvoicePayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingInvoicePaymentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoicePaymentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoicePaymentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoicePaymentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/incomingInvoicePayment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingInvoicePaymentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalIncomingInvoicePaymentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalIncomingInvoicePaymentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalIncomingInvoicePaymentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/incomingInvoicePayment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "invoiceItemAccountingCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceItemAccountingCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceItemAccountingCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalInvoiceItemAccountingCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/invoiceItemAccounting`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "invoiceItemAccountingRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceItemAccountingRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceItemAccountingRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/invoiceItemAccounting`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "invoiceItemAccountingUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceItemAccountingUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceItemAccountingUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalInvoiceItemAccountingUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/invoiceItemAccounting`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "invoiceItemAccountingDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalInvoiceItemAccountingDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceItemAccountingDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceItemAccountingDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/invoiceItemAccounting/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "invoiceItemAccountingReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalInvoiceItemAccountingReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceItemAccountingReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceItemAccountingReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/invoiceItemAccounting/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "invoiceReferenceCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceReferenceCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceReferenceCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalInvoiceReferenceCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/invoiceReference`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "invoiceReferenceRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceReferenceRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceReferenceRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/invoiceReference`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "invoiceReferenceUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceReferenceUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceReferenceUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalInvoiceReferenceUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/invoiceReference`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "invoiceReferenceDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalInvoiceReferenceDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceReferenceDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceReferenceDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/invoiceReference/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "invoiceReferenceReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalInvoiceReferenceReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceReferenceReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceReferenceReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/invoiceReference/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "invoiceSeriesCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceSeriesCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceSeriesCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalInvoiceSeriesCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/invoiceSeries`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "invoiceSeriesRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceSeriesRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceSeriesRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/invoiceSeries`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "invoiceSeriesUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceSeriesUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceSeriesUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalInvoiceSeriesUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/invoiceSeries`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "invoiceSeriesDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalInvoiceSeriesDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceSeriesDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceSeriesDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/invoiceSeries/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "invoiceSeriesReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalInvoiceSeriesReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceSeriesReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceSeriesReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/invoiceSeries/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "invoiceSeriesOpGetNextNumber": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalInvoiceSeriesOpGetNextNumber', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceSeriesOpGetNextNumber', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceSeriesOpGetNextNumber', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/invoiceSeriesOpGetNextNumber/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "invoiceSeriesOpUpdateLastNumber": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalInvoiceSeriesOpUpdateLastNumber', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalInvoiceSeriesOpUpdateLastNumber', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalInvoiceSeriesOpUpdateLastNumber', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/invoiceSeriesOpUpdateLastNumber/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalOutgoingInvoiceCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoice`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingInvoiceRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/outgoingInvoice`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalOutgoingInvoiceUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/outgoingInvoice`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingInvoiceDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/outgoingInvoice/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/outgoingInvoice/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalOutgoingInvoiceItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoiceItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingInvoiceItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/outgoingInvoiceItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalOutgoingInvoiceItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/outgoingInvoiceItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingInvoiceItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/outgoingInvoiceItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/outgoingInvoiceItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoiceOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoiceOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceOpCancel": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceOpCancel', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceOpCancel', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceOpCancel', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalOutgoingInvoiceOpCancel', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoiceOpCancel/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingInvoiceOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalOutgoingInvoiceOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoiceOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingInvoiceOpDiscount": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceOpDiscount', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceOpDiscount', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceOpDiscount', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalOutgoingInvoiceOpDiscount', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoiceOpDiscount/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingInvoiceOpOutgoingListBind": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceOpOutgoingListBind', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceOpOutgoingListBind', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceOpOutgoingListBind', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoiceOpOutgoingListBind/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoiceOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoiceOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceOpReturn": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceOpReturn', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceOpReturn', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceOpReturn', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalOutgoingInvoiceOpReturn', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoiceOpReturn/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingInvoiceOpTaxationCalc": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoiceOpTaxationCalc', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceOpTaxationCalc', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceOpTaxationCalc', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoiceOpTaxationCalc/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoiceOpUpdatePrepared": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoiceOpUpdatePrepared', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoiceOpUpdatePrepared', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalOutgoingInvoiceOpUpdatePrepared', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoiceOpUpdatePrepared`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingInvoicePaymentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoicePaymentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoicePaymentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalOutgoingInvoicePaymentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/outgoingInvoicePayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingInvoicePaymentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoicePaymentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoicePaymentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/outgoingInvoicePayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoicePaymentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoicePaymentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoicePaymentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscalOutgoingInvoicePaymentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/outgoingInvoicePayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingInvoicePaymentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoicePaymentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoicePaymentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoicePaymentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/outgoingInvoicePayment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingInvoicePaymentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscalOutgoingInvoicePaymentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscalOutgoingInvoicePaymentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscalOutgoingInvoicePaymentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/outgoingInvoicePayment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "fiscal/taxation": {

      "taxCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/taxationTaxCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/taxation/tax`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "taxRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/taxation/tax`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/taxationTaxUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/taxation/tax`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "taxDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/taxationTaxDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/taxation/tax/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/taxationTaxReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/taxation/tax/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxOpReadByCode": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxOpReadByCode', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxOpReadByCode', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/taxation/taxOpReadByCode`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxationCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/taxationTaxationCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/taxation/taxation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "taxationRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/taxation/taxation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxationUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/taxationTaxationUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/taxation/taxation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "taxationDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/taxationTaxationDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/taxation/taxation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxationReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/taxationTaxationReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/taxation/taxation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxationOperationCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationOperationCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationOperationCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/taxationTaxationOperationCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/taxation/taxationOperation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "taxationOperationRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationOperationRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationOperationRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/taxation/taxationOperation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxationOperationUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationOperationUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationOperationUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/taxationTaxationOperationUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/taxation/taxationOperation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "taxationOperationDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/taxationTaxationOperationDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationOperationDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationOperationDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/taxation/taxationOperation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxationOperationReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/taxationTaxationOperationReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationOperationReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationOperationReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/taxation/taxationOperation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxationOperationOpReadByCode": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationOperationOpReadByCode', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationOperationOpReadByCode', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/fiscal/taxation/taxationOperationOpReadByCode`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxationRuleCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationRuleCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationRuleCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/taxationTaxationRuleCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/fiscal/taxation/taxationRule`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "taxationRuleRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationRuleRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationRuleRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/taxation/taxationRule`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxationRuleUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationRuleUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationRuleUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_fiscal/taxationTaxationRuleUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/fiscal/taxation/taxationRule`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "taxationRuleDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/taxationTaxationRuleDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationRuleDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationRuleDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/fiscal/taxation/taxationRule/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "taxationRuleReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_fiscal/taxationTaxationRuleReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_fiscal/taxationTaxationRuleReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_fiscal/taxationTaxationRuleReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/fiscal/taxation/taxationRule/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "logistic": {

      "shippingCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_logisticShippingCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_logisticShippingCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_logisticShippingCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/logistic/shipping`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "shippingRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_logisticShippingRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_logisticShippingRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/logistic/shipping`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shippingUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_logisticShippingUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_logisticShippingUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_logisticShippingUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/logistic/shipping`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "shippingDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_logisticShippingDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_logisticShippingDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_logisticShippingDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/logistic/shipping/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shippingReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_logisticShippingReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_logisticShippingReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_logisticShippingReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/logistic/shipping/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shippingItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_logisticShippingItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_logisticShippingItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_logisticShippingItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/logistic/shippingItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "shippingItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_logisticShippingItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_logisticShippingItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/logistic/shippingItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shippingItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_logisticShippingItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_logisticShippingItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_logisticShippingItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/logistic/shippingItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "shippingItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_logisticShippingItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_logisticShippingItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_logisticShippingItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/logistic/shippingItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shippingItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_logisticShippingItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_logisticShippingItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_logisticShippingItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/logistic/shippingItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shippingOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_logisticShippingOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_logisticShippingOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_logisticShippingOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/logistic/shippingOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "material": {

      "addressCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialAddressCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialAddressCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialAddressCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/address`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "addressRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialAddressRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialAddressRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/address`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "addressUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialAddressUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialAddressUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialAddressUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/address`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "addressDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialAddressDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialAddressDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialAddressDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/address/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "addressReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialAddressReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialAddressReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialAddressReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/address/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "addressProfileCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialAddressProfileCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialAddressProfileCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialAddressProfileCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/addressProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "addressProfileRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialAddressProfileRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialAddressProfileRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/addressProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "addressProfileUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialAddressProfileUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialAddressProfileUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialAddressProfileUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/addressProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "addressProfileDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialAddressProfileDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialAddressProfileDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialAddressProfileDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/addressProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "addressProfileReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialAddressProfileReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialAddressProfileReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialAddressProfileReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/addressProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "handlingUnitCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialHandlingUnitCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialHandlingUnitCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialHandlingUnitCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/handlingUnit`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "handlingUnitRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialHandlingUnitRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialHandlingUnitRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/handlingUnit`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "handlingUnitUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialHandlingUnitUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialHandlingUnitUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialHandlingUnitUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/handlingUnit`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "handlingUnitDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialHandlingUnitDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialHandlingUnitDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialHandlingUnitDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/handlingUnit/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "handlingUnitReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialHandlingUnitReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialHandlingUnitReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialHandlingUnitReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/handlingUnit/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingListCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialIncomingListCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialIncomingListCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/incomingList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingListRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialIncomingListRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/incomingList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingListUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialIncomingListUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialIncomingListUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/incomingList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingListDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialIncomingListDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialIncomingListDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/incomingList/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingListReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialIncomingListReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialIncomingListReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/incomingList/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingListItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialIncomingListItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialIncomingListItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/incomingListItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingListItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialIncomingListItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/incomingListItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingListItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialIncomingListItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialIncomingListItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/incomingListItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingListItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialIncomingListItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialIncomingListItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/incomingListItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingListItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialIncomingListItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialIncomingListItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/incomingListItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingListOpImportFromOutgoingList": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialIncomingListOpImportFromOutgoingList', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialIncomingListOpImportFromOutgoingList', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListOpImportFromOutgoingList', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialIncomingListOpImportFromOutgoingList', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/incomingListOpImportFromOutgoingList/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "incomingListOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialIncomingListOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialIncomingListOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/incomingListOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "incomingListOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialIncomingListOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialIncomingListOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialIncomingListOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/incomingListOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialInventoryCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/inventory`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "inventoryRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/inventory`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialInventoryUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/inventory`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "inventoryDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/inventory/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/inventory/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryAdjustmentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryAdjustmentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryAdjustmentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialInventoryAdjustmentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/inventoryAdjustment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "inventoryAdjustmentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryAdjustmentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryAdjustmentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/inventoryAdjustment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryAdjustmentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryAdjustmentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryAdjustmentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialInventoryAdjustmentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/inventoryAdjustment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "inventoryAdjustmentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryAdjustmentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryAdjustmentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryAdjustmentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/inventoryAdjustment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryAdjustmentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryAdjustmentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryAdjustmentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryAdjustmentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/inventoryAdjustment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryAdjustmentOpCreate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryAdjustmentOpCreate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryAdjustmentOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryAdjustmentOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/inventoryAdjustmentOpCreate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryCheckCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryCheckCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryCheckCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialInventoryCheckCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/inventoryCheck`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "inventoryCheckRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryCheckRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryCheckRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/inventoryCheck`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryCheckUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryCheckUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryCheckUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialInventoryCheckUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/inventoryCheck`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "inventoryCheckDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryCheckDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryCheckDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryCheckDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/inventoryCheck/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryCheckReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryCheckReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryCheckReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryCheckReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/inventoryCheck/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/inventoryOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/inventoryOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryOpFinish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryOpFinish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryOpFinish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryOpFinish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/inventoryOpFinish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryOpFinishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryOpFinishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryOpFinishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryOpFinishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/inventoryOpFinishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/inventoryOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/inventoryOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryOpProcess": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryOpProcess', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryOpProcess', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryOpProcess', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/inventoryOpProcess/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryOpProcessRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryOpProcessRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryOpProcessRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryOpProcessRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/inventoryOpProcessRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryOpStart": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryOpStart', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryOpStart', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryOpStart', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/inventoryOpStart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryOpStartRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryOpStartRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryOpStartRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryOpStartRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/inventoryOpStartRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryProfileCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryProfileCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryProfileCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialInventoryProfileCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/inventoryProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "inventoryProfileRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryProfileRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryProfileRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/inventoryProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryProfileUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryProfileUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryProfileUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialInventoryProfileUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/inventoryProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "inventoryProfileDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryProfileDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryProfileDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryProfileDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/inventoryProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryProfileReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryProfileReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryProfileReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryProfileReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/inventoryProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryStockCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryStockCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryStockCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialInventoryStockCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/inventoryStock`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "inventoryStockRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryStockRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryStockRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/inventoryStock`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryStockUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialInventoryStockUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryStockUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialInventoryStockUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/inventoryStock`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "inventoryStockDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryStockDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryStockDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryStockDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/inventoryStock/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryStockReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryStockReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryStockReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryStockReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/inventoryStock/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "inventoryStockOpCreate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialInventoryStockOpCreate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialInventoryStockOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialInventoryStockOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/inventoryStockOpCreate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "lotCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialLotCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialLotCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialLotCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/lot`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "lotRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialLotRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialLotRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/lot`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "lotUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialLotUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialLotUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialLotUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/lot`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "lotDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialLotDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialLotDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialLotDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/lot/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "lotReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialLotReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialLotReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialLotReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/lot/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialMovingOrderCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialMovingOrderCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/movingOrder`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "movingOrderRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialMovingOrderRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/movingOrder`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialMovingOrderUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialMovingOrderUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/movingOrder`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "movingOrderDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/movingOrder/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/movingOrder/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialMovingOrderItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/movingOrderItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialMovingOrderItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialMovingOrderItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/movingOrderItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "movingOrderItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/movingOrderItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/movingOrderItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderItemOpFinish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderItemOpFinish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderItemOpFinish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderItemOpFinish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/movingOrderItemOpFinish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderItemOpLoad": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderItemOpLoad', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderItemOpLoad', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderItemOpLoad', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialMovingOrderItemOpLoad', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/movingOrderItemOpLoad/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "movingOrderItemOpStart": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderItemOpStart', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderItemOpStart', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderItemOpStart', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/movingOrderItemOpStart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderItemOpStartRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderItemOpStartRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderItemOpStartRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderItemOpStartRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/movingOrderItemOpStartRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderOpAllocateStock": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderOpAllocateStock', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderOpAllocateStock', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderOpAllocateStock', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialMovingOrderOpAllocateStock', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/movingOrderOpAllocateStock/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "movingOrderOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/movingOrderOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/movingOrderOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderOpFinish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderOpFinish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderOpFinish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderOpFinish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/movingOrderOpFinish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/movingOrderOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/movingOrderOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderOpStart": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderOpStart', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderOpStart', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderOpStart', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/movingOrderOpStart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderOpStartRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderOpStartRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderOpStartRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderOpStartRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/movingOrderOpStartRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "movingOrderOpUnload": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialMovingOrderOpUnload', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialMovingOrderOpUnload', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialMovingOrderOpUnload', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/movingOrderOpUnload/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingListRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialOutgoingListRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialOutgoingListRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/outgoingList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingListDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialOutgoingListDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialOutgoingListDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialOutgoingListDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/outgoingList/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingListReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialOutgoingListReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialOutgoingListReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialOutgoingListReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/outgoingList/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingListOpCreateFromReservation": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialOutgoingListOpCreateFromReservation', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialOutgoingListOpCreateFromReservation', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialOutgoingListOpCreateFromReservation', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialOutgoingListOpCreateFromReservation', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/outgoingListOpCreateFromReservation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingListOpMerge": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialOutgoingListOpMerge', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialOutgoingListOpMerge', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialOutgoingListOpMerge', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/outgoingListOpMerge`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingListOpOutgoingInvoiceCreate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialOutgoingListOpOutgoingInvoiceCreate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialOutgoingListOpOutgoingInvoiceCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialOutgoingListOpOutgoingInvoiceCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialOutgoingListOpOutgoingInvoiceCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/outgoingListOpOutgoingInvoiceCreate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "outgoingListOpOutgoingInvoiceCreateRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialOutgoingListOpOutgoingInvoiceCreateRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialOutgoingListOpOutgoingInvoiceCreateRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialOutgoingListOpOutgoingInvoiceCreateRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/outgoingListOpOutgoingInvoiceCreateRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingListOpPacked": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialOutgoingListOpPacked', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialOutgoingListOpPacked', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialOutgoingListOpPacked', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/outgoingListOpPacked/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingListOpPackedRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialOutgoingListOpPackedRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialOutgoingListOpPackedRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialOutgoingListOpPackedRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/outgoingListOpPackedRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "outgoingListOpVolumeCreateAuto": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialOutgoingListOpVolumeCreateAuto', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialOutgoingListOpVolumeCreateAuto', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialOutgoingListOpVolumeCreateAuto', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialOutgoingListOpVolumeCreateAuto', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/outgoingListOpVolumeCreateAuto/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "pickingOrderCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialPickingOrderCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialPickingOrderCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/pickingOrder`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "pickingOrderRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialPickingOrderRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/pickingOrder`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialPickingOrderUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialPickingOrderUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/pickingOrder`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "pickingOrderDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/pickingOrder/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/pickingOrder/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialPickingOrderItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialPickingOrderItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "pickingOrderItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialPickingOrderItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/pickingOrderItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialPickingOrderItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialPickingOrderItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/pickingOrderItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "pickingOrderItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/pickingOrderItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/pickingOrderItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialPickingOrderOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "pickingOrderOpDistribute": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpDistribute', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpDistribute', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpDistribute', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpDistribute/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpDistributeAuto": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpDistributeAuto', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpDistributeAuto', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpDistributeAuto', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpDistributeAuto/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpDistributeRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpDistributeRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpDistributeRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpDistributeRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpDistributeRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpFinish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpFinish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpFinish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpFinish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpFinish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpGroup": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpGroup', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpGroup', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialPickingOrderOpGroup', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpGroup`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "pickingOrderOpGroupRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpGroupRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpGroupRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpGroupRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpGroupRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpReservationFinish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpReservationFinish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpReservationFinish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpReservationFinish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpReservationFinish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpReservationFinishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpReservationFinishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpReservationFinishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpReservationFinishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpReservationFinishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpUngroup": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpUngroup', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpUngroup', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpUngroup', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpUngroup/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpUngroupDivergent": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpUngroupDivergent', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpUngroupDivergent', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpUngroupDivergent', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpUngroupDivergent/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingOrderOpUngroupRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingOrderOpUngroupRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingOrderOpUngroupRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingOrderOpUngroupRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/pickingOrderOpUngroupRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingProfileCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialPickingProfileCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingProfileCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialPickingProfileCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/pickingProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "pickingProfileRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialPickingProfileRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingProfileRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/pickingProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingProfileUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialPickingProfileUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingProfileUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialPickingProfileUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/pickingProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "pickingProfileDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingProfileDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingProfileDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingProfileDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/pickingProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "pickingProfileReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialPickingProfileReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialPickingProfileReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialPickingProfileReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/pickingProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "qualityCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialQualityCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialQualityCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialQualityCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/quality`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "qualityRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialQualityRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialQualityRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/quality`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "qualityUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialQualityUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialQualityUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialQualityUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/quality`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "qualityDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialQualityDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialQualityDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialQualityDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/quality/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "qualityReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialQualityReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialQualityReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialQualityReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/quality/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialReservationCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialReservationCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/reservation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "reservationRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialReservationRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/reservation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialReservationUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialReservationUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/reservation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "reservationDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/reservation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/reservation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialReservationItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/reservationItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/reservationItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpAllocate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpAllocate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpAllocate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpAllocate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialReservationOpAllocate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/reservationOpAllocate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "reservationOpAllocateAuto": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpAllocateAuto', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpAllocateAuto', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpAllocateAuto', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpAllocateAuto/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpAllocateAutoRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpAllocateAutoRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpAllocateAutoRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpAllocateAutoRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpAllocateAutoRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpAllocateRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpAllocateRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpAllocateRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpAllocateRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialReservationOpAllocateRevert', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/reservationOpAllocateRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "reservationOpAllocateStock": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpAllocateStock', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpAllocateStock', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpAllocateStock', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpAllocateStock/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpAllocateStockRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpAllocateStockRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpAllocateStockRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpAllocateStockRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpAllocateStockRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialReservationOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialReservationOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/reservationOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "reservationOpFinish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpFinish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpFinish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpFinish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpFinish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpFinishDivergent": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpFinishDivergent', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpFinishDivergent', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpFinishDivergent', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpFinishDivergent/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpFinishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpFinishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpFinishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpFinishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpFinishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpStart": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpStart', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpStart', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpStart', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpStart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationOpStartRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationOpStartRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationOpStartRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationOpStartRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/reservationOpStartRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationTargetCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialReservationTargetCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationTargetCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialReservationTargetCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/reservationTarget`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "reservationTargetRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialReservationTargetRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationTargetRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/reservationTarget`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationTargetUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialReservationTargetUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationTargetUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialReservationTargetUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/reservationTarget`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "reservationTargetDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationTargetDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationTargetDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationTargetDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/reservationTarget/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reservationTargetReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialReservationTargetReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialReservationTargetReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialReservationTargetReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/reservationTarget/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "serialCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialSerialCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialSerialCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialSerialCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/serial`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "serialRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialSerialRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialSerialRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/serial`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "serialUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialSerialUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialSerialUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialSerialUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/serial`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "serialDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialSerialDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialSerialDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialSerialDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/serial/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "serialReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialSerialReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialSerialReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialSerialReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/serial/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/stock`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialStockReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialStockReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/stock/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockAvailabilityRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockAvailabilityRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockAvailabilityRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/stockAvailability`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockClusterCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockClusterCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockClusterCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialStockClusterCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/stockCluster`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stockClusterRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockClusterRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockClusterRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/stockCluster`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockClusterUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockClusterUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockClusterUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialStockClusterUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/stockCluster`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stockClusterDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialStockClusterDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialStockClusterDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockClusterDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/stockCluster/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockClusterReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialStockClusterReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialStockClusterReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockClusterReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/stockCluster/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockManagementCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockManagementCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockManagementCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialStockManagementCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/stockManagement`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stockManagementRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockManagementRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockManagementRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/stockManagement`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockManagementUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockManagementUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockManagementUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialStockManagementUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/stockManagement`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stockManagementDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialStockManagementDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialStockManagementDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockManagementDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/stockManagement/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockManagementReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialStockManagementReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialStockManagementReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockManagementReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/stockManagement/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockManagementItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockManagementItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockManagementItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialStockManagementItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/stockManagementItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stockManagementItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockManagementItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockManagementItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/stockManagementItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockManagementItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockManagementItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockManagementItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialStockManagementItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/stockManagementItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stockManagementItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialStockManagementItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialStockManagementItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockManagementItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/stockManagementItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockManagementItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialStockManagementItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialStockManagementItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockManagementItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/stockManagementItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockManagementOpImport": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockManagementOpImport', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockManagementOpImport', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialStockManagementOpImport', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/stockManagementOpImport`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stockManagementOpUpdateAverageDailyConsumption": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockManagementOpUpdateAverageDailyConsumption', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockManagementOpUpdateAverageDailyConsumption', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialStockManagementOpUpdateAverageDailyConsumption', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/stockManagementOpUpdateAverageDailyConsumption`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stockOpBarcode": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockOpBarcode', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockOpBarcode', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialStockOpBarcode', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/stockOpBarcode`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stockOpCompensate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockOpCompensate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockOpCompensate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialStockOpCompensate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/stockOpCompensate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stockOpUpdate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialStockOpUpdate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialStockOpUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockOpUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialStockOpUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/stockOpUpdate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "stockTransactionRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialStockTransactionRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockTransactionRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/stockTransaction`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockTransactionReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialStockTransactionReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialStockTransactionReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockTransactionReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/stockTransaction/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "stockTransactionOpRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialStockTransactionOpRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialStockTransactionOpRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialStockTransactionOpRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/stockTransactionOpRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "volumeCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialVolumeCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialVolumeCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/volume`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "volumeRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialVolumeRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/volume`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "volumeUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialVolumeUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialVolumeUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/volume`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "volumeDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialVolumeDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialVolumeDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/volume/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "volumeReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialVolumeReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialVolumeReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/volume/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "volumeItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialVolumeItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialVolumeItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/volumeItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "volumeItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialVolumeItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/volumeItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "volumeItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialVolumeItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialVolumeItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/volumeItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "volumeItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialVolumeItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialVolumeItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/volumeItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "volumeOpCheck": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialVolumeOpCheck', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialVolumeOpCheck', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeOpCheck', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialVolumeOpCheck', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/volumeOpCheck/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "volumeOpCheckRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialVolumeOpCheckRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialVolumeOpCheckRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeOpCheckRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/volumeOpCheckRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "volumeOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialVolumeOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialVolumeOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/volumeOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "volumeOpHandlingUnitSet": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialVolumeOpHandlingUnitSet', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialVolumeOpHandlingUnitSet', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialVolumeOpHandlingUnitSet', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/material/volumeOpHandlingUnitSet/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "warehouseCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialWarehouseCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialWarehouseCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialWarehouseCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/material/warehouse`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "warehouseRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialWarehouseRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialWarehouseRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/warehouse`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "warehouseUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_materialWarehouseUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialWarehouseUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_materialWarehouseUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/material/warehouse`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "warehouseDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialWarehouseDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialWarehouseDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialWarehouseDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/material/warehouse/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "warehouseReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_materialWarehouseReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_materialWarehouseReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_materialWarehouseReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/material/warehouse/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "sale": {

      "priceFormationCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_salePriceFormationCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_salePriceFormationCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/priceFormation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceFormationRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_salePriceFormationRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/priceFormation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceFormationUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_salePriceFormationUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_salePriceFormationUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/sale/priceFormation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceFormationDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_salePriceFormationDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_salePriceFormationDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/sale/priceFormation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceFormationReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_salePriceFormationReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_salePriceFormationReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/priceFormation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceFormationItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_salePriceFormationItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_salePriceFormationItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/priceFormationItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceFormationItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_salePriceFormationItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/priceFormationItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceFormationItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_salePriceFormationItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_salePriceFormationItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/sale/priceFormationItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceFormationItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_salePriceFormationItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_salePriceFormationItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/sale/priceFormationItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceFormationItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_salePriceFormationItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_salePriceFormationItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/priceFormationItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceFormationOpCalculate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_salePriceFormationOpCalculate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_salePriceFormationOpCalculate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationOpCalculate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_salePriceFormationOpCalculate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/priceFormationOpCalculate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceFormationOpClone": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_salePriceFormationOpClone', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_salePriceFormationOpClone', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationOpClone', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/priceFormationOpClone/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceFormationOpSimulation": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_salePriceFormationOpSimulation', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_salePriceFormationOpSimulation', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceFormationOpSimulation', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_salePriceFormationOpSimulation', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/priceFormationOpSimulation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceListCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_salePriceListCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceListCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_salePriceListCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/priceList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceListRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_salePriceListRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceListRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/priceList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_salePriceListUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceListUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_salePriceListUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/sale/priceList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceListDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_salePriceListDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_salePriceListDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceListDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/sale/priceList/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_salePriceListReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_salePriceListReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceListReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/priceList/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_salePriceListItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceListItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_salePriceListItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/priceListItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceListItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_salePriceListItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceListItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/priceListItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_salePriceListItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceListItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_salePriceListItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/sale/priceListItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceListItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_salePriceListItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_salePriceListItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceListItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/sale/priceListItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_salePriceListItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_salePriceListItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_salePriceListItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/priceListItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleQuoteCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleQuoteCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/quote`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleQuoteRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/quote`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleQuoteUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleQuoteUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/sale/quote`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/sale/quote/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/quote/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleQuoteItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleQuoteItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/quoteItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleQuoteItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/quoteItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleQuoteItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleQuoteItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/sale/quoteItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/sale/quoteItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/quoteItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemOpReject": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteItemOpReject', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteItemOpReject', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemOpReject', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteItemOpReject/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemOpRejectRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteItemOpRejectRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteItemOpRejectRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemOpRejectRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteItemOpRejectRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemProposalCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleQuoteItemProposalCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemProposalCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleQuoteItemProposalCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/quoteItemProposal`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteItemProposalRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleQuoteItemProposalRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemProposalRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/quoteItemProposal`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemProposalUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleQuoteItemProposalUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemProposalUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleQuoteItemProposalUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/sale/quoteItemProposal`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteItemProposalDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteItemProposalDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteItemProposalDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemProposalDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/sale/quoteItemProposal/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemProposalReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteItemProposalReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteItemProposalReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemProposalReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/quoteItemProposal/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemProposalOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteItemProposalOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteItemProposalOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemProposalOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteItemProposalOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemProposalOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteItemProposalOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteItemProposalOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemProposalOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteItemProposalOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemProposalOpReject": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteItemProposalOpReject', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteItemProposalOpReject', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemProposalOpReject', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteItemProposalOpReject/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemProposalOpRejectRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteItemProposalOpRejectRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteItemProposalOpRejectRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteItemProposalOpRejectRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteItemProposalOpRejectRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpCancel": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpCancel', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpCancel', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpCancel', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpCancel/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpCancelRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpCancelRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpCancelRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpCancelRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpCancelRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleQuoteOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleQuoteOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteOpFill": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpFill', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpFill', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpFill', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpFill/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpFillRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpFillRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpFillRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpFillRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpFillRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpReject": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpReject', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpReject', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpReject', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpReject/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpRejectRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpRejectRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpRejectRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpRejectRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpRejectRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpStart": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpStart', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpStart', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpStart', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpStart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpStartRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpStartRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpStartRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpStartRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpStartRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpSubmit": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpSubmit', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpSubmit', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpSubmit', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpSubmit/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpSubmitRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleQuoteOpSubmitRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleQuoteOpSubmitRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleQuoteOpSubmitRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/quoteOpSubmitRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/sale`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/sale`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/sale/sale`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/sale/sale/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/sale/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/saleItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/saleItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/sale/saleItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/sale/saleItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/saleItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleItemOpUpdatePrepared": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleItemOpUpdatePrepared', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleItemOpUpdatePrepared', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleItemOpUpdatePrepared', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/saleItemOpUpdatePrepared`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpApproveUnconditionally": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpApproveUnconditionally', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpApproveUnconditionally', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpApproveUnconditionally', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpApproveUnconditionally/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpCancel": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpCancel', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpCancel', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpCancel', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleOpCancel', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/saleOpCancel/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleOpCancelRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpCancelRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpCancelRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpCancelRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpCancelRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpClone": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpClone', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpClone', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpClone', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpClone/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/saleOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleOpFinishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpFinishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpFinishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpFinishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpFinishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpForwardAuto": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpForwardAuto', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpForwardAuto', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpForwardAuto', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleOpForwardAuto', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/saleOpForwardAuto/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleOpMerge": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleOpMerge', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpMerge', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleOpMerge', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/saleOpMerge`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleOpMergeRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpMergeRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpMergeRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpMergeRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpMergeRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpPickingOrderCreate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpPickingOrderCreate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpPickingOrderCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpPickingOrderCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleOpPickingOrderCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/saleOpPickingOrderCreate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleOpPickingOrderCreateRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpPickingOrderCreateRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpPickingOrderCreateRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpPickingOrderCreateRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpPickingOrderCreateRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpShipmentAssign": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpShipmentAssign', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpShipmentAssign', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpShipmentAssign', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpShipmentAssign/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpShipmentAssignRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpShipmentAssignRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpShipmentAssignRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpShipmentAssignRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpShipmentAssignRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpSplit": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpSplit', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpSplit', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpSplit', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleOpSplit', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/saleOpSplit/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleOpTaxationCalc": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleOpTaxationCalc', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleOpTaxationCalc', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpTaxationCalc', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/sale/saleOpTaxationCalc/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleOpUpdatePrepared": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleOpUpdatePrepared', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleOpUpdatePrepared', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleOpUpdatePrepared', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/saleOpUpdatePrepared`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "salePaymentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSalePaymentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSalePaymentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSalePaymentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/salePayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "salePaymentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSalePaymentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSalePaymentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/salePayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "salePaymentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSalePaymentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSalePaymentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSalePaymentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/sale/salePayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "salePaymentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSalePaymentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSalePaymentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSalePaymentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/sale/salePayment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "salePaymentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSalePaymentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSalePaymentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSalePaymentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/salePayment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleProfileCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleProfileCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleProfileCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleProfileCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/sale/saleProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleProfileRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleProfileRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleProfileRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/saleProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleProfileUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_saleSaleProfileUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleProfileUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_saleSaleProfileUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/sale/saleProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "saleProfileDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleProfileDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleProfileDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleProfileDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/sale/saleProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "saleProfileReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_saleSaleProfileReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_saleSaleProfileReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_saleSaleProfileReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/sale/saleProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "shipping": {

      "shipmentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_shippingShipmentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_shippingShipmentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/shipping/shipment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "shipmentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_shippingShipmentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/shipping/shipment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_shippingShipmentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_shippingShipmentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/shipping/shipment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "shipmentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/shipping/shipment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/shipping/shipment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpCheckFinish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpCheckFinish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpCheckFinish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpCheckFinish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpCheckFinish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpCheckFinishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpCheckFinishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpCheckFinishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpCheckFinishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpCheckFinishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpCheckIgnore": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpCheckIgnore', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpCheckIgnore', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpCheckIgnore', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpCheckIgnore/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpCheckStart": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpCheckStart', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpCheckStart', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpCheckStart', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpCheckStart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpCheckStartRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpCheckStartRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpCheckStartRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpCheckStartRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpCheckStartRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpFinish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpFinish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpFinish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpFinish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpFinish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpFinishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpFinishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpFinishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpFinishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpFinishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpLoadIgnore": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpLoadIgnore', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpLoadIgnore', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpLoadIgnore', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpLoadIgnore/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpLoadStart": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpLoadStart', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpLoadStart', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpLoadStart', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpLoadStart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpLoadStartRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpLoadStartRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpLoadStartRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpLoadStartRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpLoadStartRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpLoadVolume": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpLoadVolume', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpLoadVolume', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpLoadVolume', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpLoadVolume/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpOutgoingInvoiceBind": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpOutgoingInvoiceBind', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpOutgoingInvoiceBind', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpOutgoingInvoiceBind', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpOutgoingInvoiceBind/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpOutgoingInvoiceBindRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpOutgoingInvoiceBindRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpOutgoingInvoiceBindRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpOutgoingInvoiceBindRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpOutgoingInvoiceBindRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_shippingShipmentOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_shippingShipmentOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_shippingShipmentOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/shipping/shipmentOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "supply/production": {

      "bomCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionBomCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionBomCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionBomCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/bom`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "bomRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionBomRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionBomRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/bom`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "bomUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionBomUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionBomUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionBomUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/production/bom`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "bomDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionBomDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionBomDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionBomDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/production/bom/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "bomReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionBomReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionBomReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionBomReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/bom/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "bomItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionBomItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionBomItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionBomItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/bomItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "bomItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionBomItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionBomItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/bomItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "bomItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionBomItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionBomItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionBomItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/production/bomItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "bomItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionBomItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionBomItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionBomItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/production/bomItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "bomItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionBomItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionBomItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionBomItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/bomItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "bomOpClone": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionBomOpClone', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionBomOpClone', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionBomOpClone', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/bomOpClone/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "operationCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionOperationCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionOperationCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/operation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "operationRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionOperationRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/operation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "operationUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionOperationUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionOperationUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/production/operation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "operationDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionOperationDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionOperationDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/production/operation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "operationReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionOperationReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionOperationReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/operation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "operationTypeCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionOperationTypeCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationTypeCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionOperationTypeCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/operationType`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "operationTypeRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionOperationTypeRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationTypeRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/operationType`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "operationTypeUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionOperationTypeUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationTypeUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionOperationTypeUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/production/operationType`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "operationTypeDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionOperationTypeDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionOperationTypeDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationTypeDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/production/operationType/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "operationTypeReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionOperationTypeReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionOperationTypeReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationTypeReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/operationType/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "operationTypeAccountCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionOperationTypeAccountCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationTypeAccountCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionOperationTypeAccountCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/operationTypeAccount`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "operationTypeAccountRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionOperationTypeAccountRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationTypeAccountRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/operationTypeAccount`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "operationTypeAccountUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionOperationTypeAccountUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationTypeAccountUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionOperationTypeAccountUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/production/operationTypeAccount`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "operationTypeAccountDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionOperationTypeAccountDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionOperationTypeAccountDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationTypeAccountDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/production/operationTypeAccount/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "operationTypeAccountReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionOperationTypeAccountReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionOperationTypeAccountReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionOperationTypeAccountReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/operationTypeAccount/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionBatchCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionBatchCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionBatchCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionBatchCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/productionBatch`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionBatchRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionBatchRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionBatchRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/productionBatch`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionBatchUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionBatchUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionBatchUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionBatchUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/production/productionBatch`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionBatchDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionBatchDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionBatchDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionBatchDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/production/productionBatch/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionBatchReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionBatchReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionBatchReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionBatchReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/productionBatch/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionBatchOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionBatchOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionBatchOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionBatchOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionBatchOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionBatchOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionBatchOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionBatchOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionBatchOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionBatchOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionBatchOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionBatchOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionBatchOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionBatchOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionBatchOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionBatchOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionBatchOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionBatchOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionBatchOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionBatchOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionOrderCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionOrderCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionOrderCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionOrderCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/productionOrder`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionOrderRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionOrderRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionOrderRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/productionOrder`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionOrderUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionOrderUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionOrderUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionOrderUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/production/productionOrder`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionOrderDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionOrderDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionOrderDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionOrderDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/production/productionOrder/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionOrderReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionOrderReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionOrderReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionOrderReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/productionOrder/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionOrderOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionOrderOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionOrderOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionOrderOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionOrderOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionOrderOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionOrderOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionOrderOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionOrderOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionOrderOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionOrderOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionOrderOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionOrderOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionOrderOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/productionOrderOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionOrderOpFinish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionOrderOpFinish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionOrderOpFinish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionOrderOpFinish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionOrderOpFinish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionOrderOpFinishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionOrderOpFinishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionOrderOpFinishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionOrderOpFinishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionOrderOpFinishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionOrderOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionOrderOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionOrderOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionOrderOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionOrderOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionOrderOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionOrderOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionOrderOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionOrderOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionOrderOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionProfileCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionProfileCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionProfileCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionProfileCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/productionProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionProfileRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionProfileRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionProfileRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/productionProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionProfileUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionProfileUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionProfileUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionProfileUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/production/productionProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionProfileDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionProfileDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionProfileDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionProfileDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/production/productionProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionProfileReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionProfileReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionProfileReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionProfileReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/productionProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionStepCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/productionStep`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionStepRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/productionStep`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionStepUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/production/productionStep`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionStepDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionStepDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/production/productionStep/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionStepReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/productionStep/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepConsumptionCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepConsumptionCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepConsumptionCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionStepConsumptionCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/productionStepConsumption`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionStepConsumptionRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepConsumptionRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepConsumptionRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/productionStepConsumption`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepConsumptionUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepConsumptionUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepConsumptionUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionStepConsumptionUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/production/productionStepConsumption`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionStepConsumptionDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionStepConsumptionDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepConsumptionDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepConsumptionDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/production/productionStepConsumption/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepConsumptionReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionStepConsumptionReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepConsumptionReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepConsumptionReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/productionStepConsumption/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepConsumptionOpCalculate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionStepConsumptionOpCalculate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepConsumptionOpCalculate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepConsumptionOpCalculate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionStepConsumptionOpCalculate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionStepOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/productionStepOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionStepOpFinish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionStepOpFinish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepOpFinish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepOpFinish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionStepOpFinish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepOpFinishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionStepOpFinishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepOpFinishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepOpFinishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionStepOpFinishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepOpStart": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionStepOpStart', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepOpStart', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepOpStart', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionStepOpStart/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepOpStartRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionStepOpStartRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepOpStartRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepOpStartRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/production/productionStepOpStartRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepProductionCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepProductionCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepProductionCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionStepProductionCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/production/productionStepProduction`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionStepProductionRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepProductionRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepProductionRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/productionStepProduction`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepProductionUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepProductionUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepProductionUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/productionProductionStepProductionUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/production/productionStepProduction`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "productionStepProductionDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionStepProductionDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepProductionDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepProductionDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/production/productionStepProduction/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "productionStepProductionReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/productionProductionStepProductionReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/productionProductionStepProductionReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/productionProductionStepProductionReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/production/productionStepProduction/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "supply/purchase": {

      "priceListCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePriceListCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePriceListCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePriceListCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/priceList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceListRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePriceListRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePriceListRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/priceList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePriceListUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePriceListUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePriceListUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/purchase/priceList`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceListDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePriceListDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePriceListDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePriceListDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/purchase/priceList/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePriceListReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePriceListReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePriceListReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/priceList/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePriceListItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePriceListItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePriceListItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/priceListItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceListItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePriceListItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePriceListItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/priceListItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePriceListItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePriceListItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePriceListItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/purchase/priceListItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "priceListItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePriceListItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePriceListItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePriceListItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/purchase/priceListItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "priceListItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePriceListItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePriceListItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePriceListItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/priceListItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseProposalCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/proposal`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "proposalRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/proposal`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseProposalUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/purchase/proposal`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "proposalDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/purchase/proposal/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/proposal/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseProposalItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/proposalItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "proposalItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/proposalItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseProposalItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/purchase/proposalItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "proposalItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/purchase/proposalItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/proposalItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalItemOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalItemOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalItemOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalItemOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseProposalItemOpApprove', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/proposalItemOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "proposalItemOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalItemOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalItemOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalItemOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/proposalItemOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalItemOpDisapprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalItemOpDisapprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalItemOpDisapprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalItemOpDisapprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseProposalItemOpDisapprove', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/proposalItemOpDisapprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "proposalItemOpDisapproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalItemOpDisapproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalItemOpDisapproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalItemOpDisapproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/proposalItemOpDisapproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalOpCancel": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalOpCancel', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalOpCancel', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalOpCancel', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/proposalOpCancel/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalOpCancelRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalOpCancelRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalOpCancelRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalOpCancelRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/proposalOpCancelRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/proposalOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/proposalOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "proposalRequestUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalRequestUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalRequestUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseProposalRequestUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/purchase/proposalRequest`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "proposalRequestReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseProposalRequestReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseProposalRequestReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseProposalRequestReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/proposalRequest/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchaseCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchase`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchaseRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/purchase`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchaseUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/purchase/purchase`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchaseDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/purchase/purchase/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/purchase/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchaseItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchaseItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/purchaseItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchaseItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/purchase/purchaseItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchaseItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/purchase/purchaseItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/purchaseItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseOpApproveUnconditionally": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseOpApproveUnconditionally', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpApproveUnconditionally', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpApproveUnconditionally', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpApproveUnconditionally/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseOpCancel": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseOpCancel', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpCancel', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpCancel', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchaseOpCancel', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpCancel/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchaseOpCancelRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseOpCancelRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpCancelRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpCancelRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpCancelRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseOpClone": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseOpClone', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpClone', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpClone', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpClone/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchaseOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchaseOpFinish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseOpFinish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpFinish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpFinish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpFinish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseOpFinishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseOpFinishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpFinishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpFinishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpFinishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseOpMerge": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpMerge', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpMerge', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchaseOpMerge', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpMerge`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchaseOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseOpSplit": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseOpSplit', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpSplit', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpSplit', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchaseOpSplit', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpSplit/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchaseOpTaxationCalc": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseOpTaxationCalc', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpTaxationCalc', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpTaxationCalc', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpTaxationCalc/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseOpUpdatePrepared": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseOpUpdatePrepared', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseOpUpdatePrepared', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchaseOpUpdatePrepared', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseOpUpdatePrepared`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchasePaymentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchasePaymentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchasePaymentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchasePaymentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchasePayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchasePaymentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchasePaymentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchasePaymentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/purchasePayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchasePaymentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchasePaymentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchasePaymentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchasePaymentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/purchase/purchasePayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchasePaymentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchasePaymentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchasePaymentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchasePaymentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/purchase/purchasePayment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchasePaymentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchasePaymentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchasePaymentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchasePaymentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/purchasePayment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseProfileCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseProfileCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseProfileCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchaseProfileCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/purchaseProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchaseProfileRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseProfileRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseProfileRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/purchaseProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseProfileUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseProfileUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseProfileUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchasePurchaseProfileUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/purchase/purchaseProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "purchaseProfileDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseProfileDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseProfileDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseProfileDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/purchase/purchaseProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "purchaseProfileReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchasePurchaseProfileReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchasePurchaseProfileReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchasePurchaseProfileReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/purchaseProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseQuoteCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/quote`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/quote`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseQuoteUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/purchase/quote`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseQuoteDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/purchase/quote/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseQuoteReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/quote/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseQuoteItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/quoteItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/quoteItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseQuoteItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/supply/purchase/quoteItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseQuoteItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/supply/purchase/quoteItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseQuoteItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/supply/purchase/quoteItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseQuoteOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/quoteOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseQuoteOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/quoteOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpCancel": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseQuoteOpCancel', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteOpCancel', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteOpCancel', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/quoteOpCancel/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpCancelRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseQuoteOpCancelRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteOpCancelRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteOpCancelRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/quoteOpCancelRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseQuoteOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/quoteOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteOpFinish": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseQuoteOpFinish', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteOpFinish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteOpFinish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_supply/purchaseQuoteOpFinish', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/supply/purchase/quoteOpFinish/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "quoteOpFinishRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseQuoteOpFinishRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteOpFinishRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteOpFinishRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/quoteOpFinishRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseQuoteOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/quoteOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "quoteOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_supply/purchaseQuoteOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_supply/purchaseQuoteOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_supply/purchaseQuoteOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/supply/purchase/quoteOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "system/audit": {

      "logCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/auditLogCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/auditLogCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/auditLogCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/audit/log`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "logRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/auditLogRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/auditLogRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/audit/log`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "logReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/auditLogReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/auditLogReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/auditLogReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/audit/log/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "logOpDeleteExpired": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/auditLogOpDeleteExpired', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/auditLogOpDeleteExpired', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/audit/logOpDeleteExpired`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "sourceRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/auditSourceRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/auditSourceRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/audit/source`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "sourceReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/auditSourceReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/auditSourceReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/auditSourceReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/audit/source/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "sourceOpReadByEntity": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/auditSourceOpReadByEntity', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/auditSourceOpReadByEntity', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/audit/sourceOpReadByEntity`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userLogCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/auditUserLogCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/auditUserLogCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/auditUserLogCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/audit/userLog`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userLogRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/auditUserLogRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/auditUserLogRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/audit/userLog`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userLogUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/auditUserLogUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/auditUserLogUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/auditUserLogUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/audit/userLog`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userLogDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/auditUserLogDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/auditUserLogDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/auditUserLogDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/audit/userLog/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userLogReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/auditUserLogReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/auditUserLogReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/auditUserLogReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/audit/userLog/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "system/automation": {

      "agentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/automationAgentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationAgentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/automationAgentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/automation/agent`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "agentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/automationAgentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationAgentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/automation/agent`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "agentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/automationAgentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationAgentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/automationAgentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/automation/agent`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "agentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/automationAgentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/automationAgentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationAgentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/automation/agent/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "agentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/automationAgentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/automationAgentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationAgentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/automation/agent/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "agentOpExecute": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/automationAgentOpExecute', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/automationAgentOpExecute', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationAgentOpExecute', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/automationAgentOpExecute', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/automation/agentOpExecute/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "scheduleCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/automationScheduleCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationScheduleCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/automationScheduleCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/automation/schedule`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "scheduleRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/automationScheduleRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationScheduleRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/automation/schedule`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "scheduleUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/automationScheduleUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationScheduleUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/automationScheduleUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/automation/schedule`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "scheduleDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/automationScheduleDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/automationScheduleDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationScheduleDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/automation/schedule/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "scheduleReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/automationScheduleReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/automationScheduleReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationScheduleReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/automation/schedule/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "scheduleOpExecute": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/automationScheduleOpExecute', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/automationScheduleOpExecute', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationScheduleOpExecute', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/automation/scheduleOpExecute/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "watcherCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/automationWatcherCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationWatcherCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/automationWatcherCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/automation/watcher`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "watcherRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/automationWatcherRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationWatcherRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/automation/watcher`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "watcherUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/automationWatcherUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationWatcherUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/automationWatcherUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/automation/watcher`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "watcherDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/automationWatcherDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/automationWatcherDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationWatcherDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/automation/watcher/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "watcherReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/automationWatcherReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/automationWatcherReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/automationWatcherReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/automation/watcher/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "system": {

      "checkAuth": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_systemCheckAuth', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemCheckAuth', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/checkAuth`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "customPropertiesOpSet": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_systemCustomPropertiesOpSet', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemCustomPropertiesOpSet', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_systemCustomPropertiesOpSet', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/customPropertiesOpSet`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "customTagsOpSet": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_systemCustomTagsOpSet', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemCustomTagsOpSet', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/customTagsOpSet`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "databaseOpOptimize": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_systemDatabaseOpOptimize', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemDatabaseOpOptimize', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/databaseOpOptimize`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "infoRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_systemInfoRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemInfoRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/info`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "lockOpAcquire": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_systemLockOpAcquire', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_systemLockOpAcquire', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemLockOpAcquire', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/lockOpAcquire/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "lockOpExtend": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_systemLockOpExtend', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_systemLockOpExtend', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemLockOpExtend', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/lockOpExtend/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "lockOpRelease": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_systemLockOpRelease', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_systemLockOpRelease', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemLockOpRelease', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/lockOpRelease/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "resourcesRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_systemResourcesRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemResourcesRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/resources`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "statusCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_systemStatusCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemStatusCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_systemStatusCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/status`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "statusRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_systemStatusRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemStatusRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/status`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "statusUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_systemStatusUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemStatusUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_systemStatusUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/status`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "statusDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_systemStatusDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_systemStatusDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemStatusDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/status/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "statusReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_systemStatusReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_systemStatusReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemStatusReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/status/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "testCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_systemTestCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemTestCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_systemTestCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/test`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "testRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_systemTestRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_systemTestRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/test`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "system/data": {

      "dataSourceCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/dataDataSourceCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/data/dataSource`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dataSourceRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/data/dataSource`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dataSourceUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/dataDataSourceUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/data/dataSource`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dataSourceDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/dataDataSourceDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/data/dataSource/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dataSourceReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/dataDataSourceReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/data/dataSource/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dataSourceFieldRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceFieldRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceFieldRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/data/dataSourceField`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dataSourceFieldUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceFieldUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceFieldUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/dataDataSourceFieldUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/data/dataSourceField`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dataSourceFieldReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/dataDataSourceFieldReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceFieldReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceFieldReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/data/dataSourceField/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dataSourceOpRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceOpRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceOpRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/dataDataSourceOpRead', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/data/dataSourceOpRead`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dataSourceParameterCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceParameterCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceParameterCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/dataDataSourceParameterCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/data/dataSourceParameter`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dataSourceParameterRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceParameterRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceParameterRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/data/dataSourceParameter`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dataSourceParameterUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceParameterUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceParameterUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/dataDataSourceParameterUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/data/dataSourceParameter`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "dataSourceParameterDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/dataDataSourceParameterDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceParameterDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceParameterDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/data/dataSourceParameter/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "dataSourceParameterReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/dataDataSourceParameterReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/dataDataSourceParameterReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/dataDataSourceParameterReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/data/dataSourceParameter/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "system/file": {

      "fileCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/fileFileCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/fileFileCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/fileFileCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/file/file`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "fileRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/fileFileRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/fileFileRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/file/file`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fileUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/fileFileUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/fileFileUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/fileFileUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/file/file`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "fileDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/fileFileDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/fileFileDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/fileFileDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/file/file/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fileReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/fileFileReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/fileFileReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/fileFileReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/file/file/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "fileOpReadContent": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/fileFileOpReadContent', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/fileFileOpReadContent', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/fileFileOpReadContent', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/file/fileOpReadContent/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "system/frontend": {

      "injectsRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/frontendInjectsRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/frontendInjectsRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/frontend/injects`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "injectsUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/frontendInjectsUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/frontendInjectsUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/frontendInjectsUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/frontend/injects`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "menuRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/frontendMenuRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/frontendMenuRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/frontend/menu`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "menuUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/frontendMenuUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/frontendMenuUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/frontendMenuUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/frontend/menu`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "resourcesRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/frontendResourcesRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/frontendResourcesRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/frontend/resources`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "resourcesUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/frontendResourcesUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/frontendResourcesUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/frontendResourcesUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/frontend/resources`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }

  },
  "system/image": {

      "imageCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/imageImageCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/imageImageCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/imageImageCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/image/image`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "imageRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/imageImageRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/imageImageRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/image/image`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "imageUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/imageImageUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/imageImageUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/imageImageUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/image/image`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "imageDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/imageImageDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/imageImageDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/imageImageDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/image/image/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "imageReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/imageImageReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/imageImageReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/imageImageReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/image/image/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "system/info": {

      "healthRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/infoHealthRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/infoHealthRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/info/health`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "metricsRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/infoMetricsRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/infoMetricsRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/info/metrics`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "system/integration": {

      "messageRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationMessageRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationMessageRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/integration/message`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "messageDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/integrationMessageDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/integrationMessageDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationMessageDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/integration/message/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "messageReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/integrationMessageReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/integrationMessageReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationMessageReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/integration/message/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "messageOpPost": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationMessageOpPost', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationMessageOpPost', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/integrationMessageOpPost', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/integration/messageOpPost`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "messageOpTransfer": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/integrationMessageOpTransfer', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/integrationMessageOpTransfer', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationMessageOpTransfer', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/integration/messageOpTransfer/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "queueCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationQueueCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationQueueCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/integrationQueueCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/integration/queue`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "queueRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationQueueRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationQueueRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/integration/queue`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "queueUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationQueueUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationQueueUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/integrationQueueUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/integration/queue`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "queueDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/integrationQueueDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/integrationQueueDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationQueueDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/integration/queue/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "queueReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/integrationQueueReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/integrationQueueReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationQueueReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/integration/queue/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "queueOpDeleteExpiredMessages": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationQueueOpDeleteExpiredMessages', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationQueueOpDeleteExpiredMessages', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/integration/queueOpDeleteExpiredMessages`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "queueOpReadMessage": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationQueueOpReadMessage', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationQueueOpReadMessage', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/integrationQueueOpReadMessage', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/integration/queueOpReadMessage`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "queueOpReadMessageTimeout": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationQueueOpReadMessageTimeout', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationQueueOpReadMessageTimeout', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/integrationQueueOpReadMessageTimeout', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/integration/queueOpReadMessageTimeout`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "subscriptionCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationSubscriptionCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationSubscriptionCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/integrationSubscriptionCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/integration/subscription`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "subscriptionRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationSubscriptionRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationSubscriptionRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/integration/subscription`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "subscriptionUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationSubscriptionUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationSubscriptionUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/integrationSubscriptionUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/integration/subscription`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "subscriptionDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/integrationSubscriptionDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/integrationSubscriptionDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationSubscriptionDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/integration/subscription/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "subscriptionReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/integrationSubscriptionReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/integrationSubscriptionReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationSubscriptionReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/integration/subscription/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "topicCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationTopicCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationTopicCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/integrationTopicCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/integration/topic`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "topicRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationTopicRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationTopicRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/integration/topic`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "topicUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationTopicUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationTopicUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/integrationTopicUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/integration/topic`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "topicDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/integrationTopicDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/integrationTopicDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationTopicDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/integration/topic/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "topicReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/integrationTopicReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/integrationTopicReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationTopicReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/integration/topic/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "topicOpPublish": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/integrationTopicOpPublish', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/integrationTopicOpPublish', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/integrationTopicOpPublish', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/integration/topicOpPublish`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }

  },
  "system/job": {

      "jobRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/jobJobRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/jobJobRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/job/job`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "jobDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/jobJobDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/jobJobDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/jobJobDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/job/job/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "jobReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/jobJobReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/jobJobReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/jobJobReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/job/job/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "system/mail": {

      "mailerConfigCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/mailMailerConfigCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/mailMailerConfigCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/mailMailerConfigCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/mail/mailerConfig`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "mailerConfigRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/mailMailerConfigRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/mailMailerConfigRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/mail/mailerConfig`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "mailerConfigUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/mailMailerConfigUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/mailMailerConfigUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/mailMailerConfigUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/mail/mailerConfig`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "mailerConfigDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/mailMailerConfigDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/mailMailerConfigDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/mailMailerConfigDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/mail/mailerConfig/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "mailerConfigReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/mailMailerConfigReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/mailMailerConfigReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/mailMailerConfigReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/mail/mailerConfig/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "messageOpSend": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/mailMessageOpSend', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/mailMessageOpSend', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/mailMessageOpSend', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/mail/messageOpSend`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }

  },
  "system/printing": {

      "printerCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/printingPrinterCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/printingPrinterCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/printing/printer`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "printerRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/printingPrinterRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/printing/printer`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/printingPrinterUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/printingPrinterUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/printing/printer`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "printerDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/printingPrinterDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/printingPrinterDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/printing/printer/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/printingPrinterReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/printingPrinterReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/printing/printer/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerJobCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/printingPrinterJobCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterJobCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/printingPrinterJobCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/printing/printerJob`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "printerJobRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/printingPrinterJobRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterJobRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/printing/printerJob`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerJobDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/printingPrinterJobDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/printingPrinterJobDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterJobDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/printing/printerJob/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerJobReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/printingPrinterJobReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/printingPrinterJobReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterJobReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/printing/printerJob/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerJobDataReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/printingPrinterJobDataReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/printingPrinterJobDataReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterJobDataReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/printing/printerJobData/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerJobOpFetch": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/printingPrinterJobOpFetch', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/printingPrinterJobOpFetch', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterJobOpFetch', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/printing/printerJobOpFetch/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerJobOpPrinted": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/printingPrinterJobOpPrinted', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/printingPrinterJobOpPrinted', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterJobOpPrinted', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/printing/printerJobOpPrinted/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerJobOpUnlock": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/printingPrinterJobOpUnlock', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/printingPrinterJobOpUnlock', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterJobOpUnlock', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/printing/printerJobOpUnlock/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerOpPause": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/printingPrinterOpPause', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/printingPrinterOpPause', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterOpPause', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/printing/printerOpPause/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerOpPauseRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/printingPrinterOpPauseRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/printingPrinterOpPauseRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterOpPauseRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/printing/printerOpPauseRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerOpPrintTestPage": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/printingPrinterOpPrintTestPage', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/printingPrinterOpPrintTestPage', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterOpPrintTestPage', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/printing/printerOpPrintTestPage/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "printerOpPrinterJobFetch": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/printingPrinterOpPrinterJobFetch', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/printingPrinterOpPrinterJobFetch', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/printingPrinterOpPrinterJobFetch', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/printing/printerOpPrinterJobFetch/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "system/report": {

      "reportCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/reportReportCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/reportReportCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/reportReportCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/report/report`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "reportRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/reportReportRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/reportReportRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/report/report`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reportUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/reportReportUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/reportReportUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/reportReportUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/report/report`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "reportDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/reportReportDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/reportReportDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/reportReportDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/report/report/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reportReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/reportReportReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/reportReportReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/reportReportReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/report/report/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reportOpClone": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/reportReportOpClone', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/reportReportOpClone', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/reportReportOpClone', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/report/reportOpClone/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reportOpGenerate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/reportReportOpGenerate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/reportReportOpGenerate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/reportReportOpGenerate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/report/reportOpGenerate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "reportOpPrint": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/reportReportOpPrint', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/reportReportOpPrint', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/reportReportOpPrint', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/report/reportOpPrint`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "reportTemplateRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/reportReportTemplateRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/reportReportTemplateRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/report/reportTemplate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reportTemplateReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/reportReportTemplateReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/reportReportTemplateReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/reportReportTemplateReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/report/reportTemplate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "reportTemplateOpUpdate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/reportReportTemplateOpUpdate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/reportReportTemplateOpUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/reportReportTemplateOpUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/reportReportTemplateOpUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/report/reportTemplateOpUpdate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }

  },
  "system/security": {

      "accessPointCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityAccessPointCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityAccessPointCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityAccessPointCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/accessPoint`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accessPointRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityAccessPointRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityAccessPointRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/accessPoint`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accessPointUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityAccessPointUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityAccessPointUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityAccessPointUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/security/accessPoint`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accessPointDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityAccessPointDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityAccessPointDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityAccessPointDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/security/accessPoint/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accessPointReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityAccessPointReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityAccessPointReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityAccessPointReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/accessPoint/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accessPointOpValidate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityAccessPointOpValidate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityAccessPointOpValidate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/security/accessPointOpValidate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accessProfileCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityAccessProfileCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityAccessProfileCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityAccessProfileCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/accessProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accessProfileRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityAccessProfileRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityAccessProfileRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/accessProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accessProfileUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityAccessProfileUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityAccessProfileUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityAccessProfileUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/security/accessProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "accessProfileDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityAccessProfileDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityAccessProfileDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityAccessProfileDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/security/accessProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "accessProfileReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityAccessProfileReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityAccessProfileReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityAccessProfileReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/accessProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "grantCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityGrantCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityGrantCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityGrantCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/grant`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "grantRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityGrantRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityGrantRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/grant`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "grantDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityGrantDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityGrantDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityGrantDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/security/grant/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "grantReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityGrantReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityGrantReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityGrantReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/grant/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "groupCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityGroupCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityGroupCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityGroupCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/group`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "groupRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityGroupRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityGroupRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/group`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "groupUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityGroupUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityGroupUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityGroupUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/security/group`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "groupDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityGroupDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityGroupDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityGroupDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/security/group/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "groupReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityGroupReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityGroupReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityGroupReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/group/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "groupOpClone": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityGroupOpClone', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityGroupOpClone', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityGroupOpClone', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityGroupOpClone', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/groupOpClone/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "keyStoreCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityKeyStoreCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityKeyStoreCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityKeyStoreCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/keyStore`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "keyStoreRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityKeyStoreRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityKeyStoreRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/keyStore`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "keyStoreUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityKeyStoreUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityKeyStoreUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityKeyStoreUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/security/keyStore`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "keyStoreDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityKeyStoreDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityKeyStoreDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityKeyStoreDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/security/keyStore/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "keyStoreReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityKeyStoreReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityKeyStoreReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityKeyStoreReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/keyStore/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "keyStoreOpReadContent": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityKeyStoreOpReadContent', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityKeyStoreOpReadContent', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityKeyStoreOpReadContent', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/security/keyStoreOpReadContent/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "roleRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityRoleRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityRoleRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/role`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "roleReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityRoleReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityRoleReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityRoleReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/role/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "roleAccessPointCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityRoleAccessPointCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityRoleAccessPointCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityRoleAccessPointCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/roleAccessPoint`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "roleAccessPointRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityRoleAccessPointRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityRoleAccessPointRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/roleAccessPoint`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "roleAccessPointDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityRoleAccessPointDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityRoleAccessPointDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityRoleAccessPointDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/security/roleAccessPoint/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "roleAccessPointReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityRoleAccessPointReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityRoleAccessPointReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityRoleAccessPointReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/roleAccessPoint/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "sessionRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securitySessionRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securitySessionRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/session`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "sessionDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securitySessionDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securitySessionDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securitySessionDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/security/session/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "sessionReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securitySessionReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securitySessionReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securitySessionReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/session/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "sessionContext": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securitySessionContext', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securitySessionContext', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/sessionContext`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "sessionOpGetCurrent": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securitySessionOpGetCurrent', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securitySessionOpGetCurrent', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/sessionOpGetCurrent`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "tokenOpInvalidate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityTokenOpInvalidate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityTokenOpInvalidate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityTokenOpInvalidate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/tokenOpInvalidate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "tokenOpInvalidateCurrent": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityTokenOpInvalidateCurrent', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityTokenOpInvalidateCurrent', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/security/tokenOpInvalidateCurrent`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "tokenOpRenew": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityTokenOpRenew', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityTokenOpRenew', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/security/tokenOpRenew`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "tokenOpRequest": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityTokenOpRequest', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityTokenOpRequest', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityTokenOpRequest', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/tokenOpRequest`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "tokenOpRequestPermanent": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityTokenOpRequestPermanent', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityTokenOpRequestPermanent', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityTokenOpRequestPermanent', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/tokenOpRequestPermanent`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/user`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/user`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/security/user`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityUserDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityUserDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/security/user/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityUserReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityUserReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/user/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userOpActivate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityUserOpActivate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityUserOpActivate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpActivate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/security/userOpActivate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userOpAuthenticate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserOpAuthenticate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpAuthenticate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserOpAuthenticate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/userOpAuthenticate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userOpChangeEmail": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserOpChangeEmail', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpChangeEmail', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserOpChangeEmail', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/userOpChangeEmail`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userOpChangeEmailConfirmation": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserOpChangeEmailConfirmation', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpChangeEmailConfirmation', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserOpChangeEmailConfirmation', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/userOpChangeEmailConfirmation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userOpChangePassword": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserOpChangePassword', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpChangePassword', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserOpChangePassword', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/userOpChangePassword`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userOpChangePasswordThird": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityUserOpChangePasswordThird', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityUserOpChangePasswordThird', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpChangePasswordThird', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserOpChangePasswordThird', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/userOpChangePasswordThird/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userOpCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserOpCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserOpCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/userOpCreate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userOpCreateConfirmation": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserOpCreateConfirmation', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpCreateConfirmation', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserOpCreateConfirmation', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/userOpCreateConfirmation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userOpCreateResend": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserOpCreateResend', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpCreateResend', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserOpCreateResend', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/userOpCreateResend`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userOpGrantOptionAdd": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityUserOpGrantOptionAdd', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityUserOpGrantOptionAdd', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpGrantOptionAdd', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/security/userOpGrantOptionAdd/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userOpGrantOptionRemove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityUserOpGrantOptionRemove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityUserOpGrantOptionRemove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpGrantOptionRemove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/security/userOpGrantOptionRemove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userOpInactivate": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityUserOpInactivate', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityUserOpInactivate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpInactivate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/security/userOpInactivate/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userOpResetPassword": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserOpResetPassword', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpResetPassword', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserOpResetPassword', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/userOpResetPassword`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userOpResetPasswordConfirmation": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserOpResetPasswordConfirmation', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpResetPasswordConfirmation', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserOpResetPasswordConfirmation', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/userOpResetPasswordConfirmation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userOpUnlock": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/securityUserOpUnlock', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/securityUserOpUnlock', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpUnlock', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/security/userOpUnlock/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userOpUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserOpUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserOpUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/securityUserOpUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/security/userOpUpdate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userPermissionRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/securityUserPermissionRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/securityUserPermissionRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/security/userPermission`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "system/storage": {

      "get": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storageGet', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storageGet', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/storage/get`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "getNumber": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storageGetNumber', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storageGetNumber', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/storage/getNumber`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "getString": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storageGetString', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storageGetString', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/storage/getString`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "put": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storagePut', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storagePut', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/storagePut', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/storage/put`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "putNumber": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storagePutNumber', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storagePutNumber', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "PUT",
        path: `/system/storage/putNumber`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "putString": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storagePutString', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storagePutString', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "PUT",
        path: `/system/storage/putString`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "remove": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storageRemove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storageRemove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/storage/remove`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userGet": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storageUserGet', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storageUserGet', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/storage/userGet`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userGetNumber": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storageUserGetNumber', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storageUserGetNumber', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/storage/userGetNumber`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userGetString": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storageUserGetString', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storageUserGetString', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/storage/userGetString`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userPut": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storageUserPut', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storageUserPut', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/storageUserPut', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/storage/userPut`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "userPutNumber": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storageUserPutNumber', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storageUserPutNumber', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "PUT",
        path: `/system/storage/userPutNumber`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userPutString": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storageUserPutString', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storageUserPutString', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "PUT",
        path: `/system/storage/userPutString`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "userRemove": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/storageUserRemove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/storageUserRemove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/storage/userRemove`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "system/workflow": {

      "workflowCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/workflowWorkflowCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/workflow/workflow`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "workflowRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/workflow/workflow`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/workflowWorkflowUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/workflow/workflow`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "workflowDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkflowDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/workflow/workflow/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkflowReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/workflow/workflow/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowConnectorCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowConnectorCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowConnectorCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/workflowWorkflowConnectorCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/workflow/workflowConnector`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "workflowConnectorRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowConnectorRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowConnectorRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/workflow/workflowConnector`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowConnectorUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowConnectorUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowConnectorUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/workflowWorkflowConnectorUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/workflow/workflowConnector`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "workflowConnectorDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkflowConnectorDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowConnectorDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowConnectorDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/workflow/workflowConnector/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowConnectorReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkflowConnectorReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowConnectorReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowConnectorReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/workflow/workflowConnector/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowNodeCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowNodeCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowNodeCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/workflowWorkflowNodeCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/workflow/workflowNode`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "workflowNodeRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowNodeRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowNodeRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/workflow/workflowNode`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowNodeUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowNodeUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowNodeUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/workflowWorkflowNodeUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/system/workflow/workflowNode`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "workflowNodeDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkflowNodeDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowNodeDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowNodeDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/system/workflow/workflowNode/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowNodeReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkflowNodeReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowNodeReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowNodeReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/workflow/workflowNode/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkflowOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/workflow/workflowOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkflowOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/workflow/workflowOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowOpCreateOrUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowOpCreateOrUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowOpCreateOrUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_system/workflowWorkflowOpCreateOrUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/system/workflow/workflowOpCreateOrUpdate`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "workflowOpDisable": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkflowOpDisable', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowOpDisable', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowOpDisable', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/workflow/workflowOpDisable/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowOpDisableRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkflowOpDisableRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowOpDisableRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowOpDisableRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/workflow/workflowOpDisableRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkflowOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/workflow/workflowOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workflowOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkflowOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkflowOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkflowOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/workflow/workflowOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workpieceRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/workflowWorkpieceRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkpieceRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/workflow/workpiece`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workpieceReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkpieceReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkpieceReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkpieceReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/workflow/workpiece/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workpieceNodeRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_system/workflowWorkpieceNodeRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkpieceNodeRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/workflow/workpieceNode`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workpieceNodeReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkpieceNodeReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkpieceNodeReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkpieceNodeReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/system/workflow/workpieceNode/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workpieceOpForward": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkpieceOpForward', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkpieceOpForward', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkpieceOpForward', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/workflow/workpieceOpForward/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "workpieceOpRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_system/workflowWorkpieceOpRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_system/workflowWorkpieceOpRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_system/workflowWorkpieceOpRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/system/workflow/workpieceOpRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  },
  "trade": {

      "clearanceCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeClearanceCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeClearanceCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/clearance`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "clearanceRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeClearanceRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/clearance`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "clearanceUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeClearanceUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeClearanceUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/clearance`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "clearanceDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeClearanceDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeClearanceDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/clearance/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "clearanceReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeClearanceReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeClearanceReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/clearance/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "clearanceItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeClearanceItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeClearanceItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/clearanceItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "clearanceItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeClearanceItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/clearanceItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "clearanceItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeClearanceItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeClearanceItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/clearanceItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "clearanceItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeClearanceItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeClearanceItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/clearanceItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "clearanceItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeClearanceItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeClearanceItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/clearanceItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "clearanceOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeClearanceOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeClearanceOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/clearanceOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "clearanceOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeClearanceOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeClearanceOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/clearanceOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "clearanceOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeClearanceOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeClearanceOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/clearanceOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "clearanceOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeClearanceOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeClearanceOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeClearanceOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/clearanceOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeContractCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeContractCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/contract`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeContractRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/contract`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeContractUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeContractUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/contract`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/contract/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/contract/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeContractItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeContractItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/contractItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeContractItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/contractItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeContractItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeContractItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/contractItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/contractItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/contractItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/contractOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/contractOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/contractOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/contractOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractPaymentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeContractPaymentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractPaymentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeContractPaymentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/contractPayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractPaymentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeContractPaymentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractPaymentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/contractPayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractPaymentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeContractPaymentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractPaymentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeContractPaymentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/contractPayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "contractPaymentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractPaymentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractPaymentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractPaymentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/contractPayment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractPaymentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractPaymentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractPaymentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractPaymentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/contractPayment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractPaymentOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractPaymentOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractPaymentOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractPaymentOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/contractPaymentOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractPaymentOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractPaymentOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractPaymentOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractPaymentOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/contractPaymentOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractPaymentOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractPaymentOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractPaymentOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractPaymentOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/contractPaymentOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "contractPaymentOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeContractPaymentOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeContractPaymentOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeContractPaymentOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/contractPaymentOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "costCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeCostCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeCostCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeCostCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/cost`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "costRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeCostRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeCostRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/cost`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "costUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeCostUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeCostUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeCostUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/cost`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "costDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeCostDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeCostDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeCostDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/cost/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "costReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeCostReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeCostReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeCostReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/cost/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "costAllocationCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeCostAllocationCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeCostAllocationCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeCostAllocationCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/costAllocation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "costAllocationRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeCostAllocationRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeCostAllocationRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/costAllocation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "costAllocationUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeCostAllocationUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeCostAllocationUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeCostAllocationUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/costAllocation`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "costAllocationDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeCostAllocationDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeCostAllocationDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeCostAllocationDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/costAllocation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "costAllocationReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeCostAllocationReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeCostAllocationReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeCostAllocationReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/costAllocation/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeExchangeContractCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/exchangeContract`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "exchangeContractRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/exchangeContract`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeExchangeContractUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/exchangeContract`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "exchangeContractDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/exchangeContract/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/exchangeContract/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/exchangeContractOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/exchangeContractOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/exchangeContractOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/exchangeContractOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractPaymentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractPaymentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractPaymentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeExchangeContractPaymentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/exchangeContractPayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "exchangeContractPaymentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractPaymentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractPaymentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/exchangeContractPayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractPaymentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractPaymentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractPaymentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeExchangeContractPaymentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/exchangeContractPayment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "exchangeContractPaymentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractPaymentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractPaymentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractPaymentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/exchangeContractPayment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractPaymentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractPaymentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractPaymentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractPaymentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/exchangeContractPayment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractPaymentOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractPaymentOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractPaymentOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractPaymentOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/exchangeContractPaymentOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractPaymentOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractPaymentOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractPaymentOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractPaymentOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/exchangeContractPaymentOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractPaymentOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractPaymentOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractPaymentOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractPaymentOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/exchangeContractPaymentOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractPaymentOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractPaymentOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractPaymentOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractPaymentOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/exchangeContractPaymentOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractProfileCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractProfileCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractProfileCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeExchangeContractProfileCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/exchangeContractProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "exchangeContractProfileRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractProfileRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractProfileRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/exchangeContractProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractProfileUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractProfileUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractProfileUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeExchangeContractProfileUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/exchangeContractProfile`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "exchangeContractProfileDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractProfileDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractProfileDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractProfileDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/exchangeContractProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "exchangeContractProfileReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeExchangeContractProfileReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeExchangeContractProfileReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeExchangeContractProfileReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/exchangeContractProfile/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeShipmentCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeShipmentCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/shipment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "shipmentRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeShipmentRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/shipment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeShipmentUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeShipmentUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/shipment`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "shipmentDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeShipmentDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeShipmentDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/shipment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeShipmentReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeShipmentReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/shipment/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentContainerCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeShipmentContainerCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentContainerCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeShipmentContainerCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/shipmentContainer`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "shipmentContainerRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeShipmentContainerRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentContainerRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/shipmentContainer`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentContainerUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeShipmentContainerUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentContainerUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeShipmentContainerUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/shipmentContainer`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "shipmentContainerDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeShipmentContainerDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeShipmentContainerDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentContainerDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/shipmentContainer/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentContainerReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeShipmentContainerReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeShipmentContainerReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentContainerReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/shipmentContainer/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentItemCreate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeShipmentItemCreate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentItemCreate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeShipmentItemCreate', i, {}) as IDataObject;

      const options: any = {
        method: "POST",
        path: `/trade/shipmentItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "shipmentItemRead": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeShipmentItemRead', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentItemRead', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/shipmentItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentItemUpdate": async function (this: IExecuteFunctions, i: number) {
      

      const rawQs = this.getNodeParameter('qs_tradeShipmentItemUpdate', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentItemUpdate', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      const body = this.getNodeParameter('body_tradeShipmentItemUpdate', i, {}) as IDataObject;

      const options: any = {
        method: "PUT",
        path: `/trade/shipmentItem`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      
      if (body && Object.keys(body).length > 0) {
        options.body = body;
      }
      

      return request.call(this, options);
    }
,
      "shipmentItemDelete": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeShipmentItemDelete', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeShipmentItemDelete', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentItemDelete', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "DELETE",
        path: `/trade/shipmentItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentItemReadById": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeShipmentItemReadById', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeShipmentItemReadById', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentItemReadById', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "GET",
        path: `/trade/shipmentItem/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpApprove": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeShipmentOpApprove', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeShipmentOpApprove', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentOpApprove', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/shipmentOpApprove/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpApproveRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeShipmentOpApproveRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeShipmentOpApproveRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentOpApproveRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/shipmentOpApproveRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpPrepare": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeShipmentOpPrepare', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeShipmentOpPrepare', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentOpPrepare', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/shipmentOpPrepare/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }
,
      "shipmentOpPrepareRevert": async function (this: IExecuteFunctions, i: number) {
      const pathParams = this.getNodeParameter('pathParams_tradeShipmentOpPrepareRevert', i, {}) as IDataObject;

      const rawQs = this.getNodeParameter('qs_tradeShipmentOpPrepareRevert', i, {}) as IDataObject;
      const qs: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawQs)) {
        if (v !== undefined && v !== null) qs[k] = String(v);
      }

      const rawHeaders = this.getNodeParameter('headers_tradeShipmentOpPrepareRevert', i, {}) as IDataObject;
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(rawHeaders)) {
        if (v !== undefined && v !== null) headers[k] = String(v);
      }

      

      const options: any = {
        method: "POST",
        path: `/trade/shipmentOpPrepareRevert/${pathParams.id}`,
      };

      if (Object.keys(qs).length > 0) {
        options.qs = qs;
      }

      if (Object.keys(headers).length > 0) {
        options.headers = headers;
      }

      

      return request.call(this, options);
    }

  }
};
